# Bounded-height variance test — 2026-09-24

Gap: exterior Laguerre signs remain missing from L266's witness target. The initial intermediate target was an explicit upper bound V_n≤C log(n+2)/(n+1), sufficient to leave finitely many indices on 7/2≤|a|≤4. A direct uniform bound V_n<1/32 would instead settle every index on that band. Continue if an actual-theta curvature estimate gives this strict threshold; abandon this shortcut if its constant remains at or below 16. Uniform signs at unbounded heights and the endpoint arithmetic margin remain separate unresolved steps.

Redundancy and previous failures: L267 records the sufficient condition 2a²V_n≤1. L268 rules it out through the linear witness cutoff at growing heights, not at fixed heights. L255 covers |a|≤7/2. L047–L048 prove only the recorded curvature constant (68/125)π at its minimum; no saved result supplies the constant needed here. The L243–L244 failures concern different tilted densities. Existing uncommitted work has been preserved.

The completed proof is [L308](../lemmas/L308-uniform-curvature-and-bounded-height-positivity.md). Retaining each summand's rational curvature term gives a mean curvature greater than 19, while a sharper summand-slope estimate and geometric tail bound make the variance less than 2. The resulting uniform bound −(log k)''>17 makes every conditional t-potential have curvature greater than 34. Integration by parts, with the actual theta derivative tails checked, gives V_n<1/34 for every n≥1.

The test passes: the required upper threshold at |a|=4 is 1/32, and 1/34 is smaller. L267 consequently gives strict signs for all indices on the larger interval |a|≤sqrt(17). This also excludes nonreal centers in that interval, including a small range beyond the previously recorded localization |Re z|>4. The initially proposed log(n)/n decay rate is not established; it is unnecessary for this particular compact band.

Assessment: ADVANCE, with zero consecutive unresolved exploration turns. The result does not meet the all-height requirement or repair the endpoint arithmetic sign. Conditional-variance monotonicity is a concrete follow-up because it could reduce the sharper bounded-height certificate to the single ratio V_1; its hypotheses still need testing. L268 remains an obstruction at growing heights, and no RH candidate is recorded. The exploratory reasoning was saved before writing the full proof; the canonical proof now replaces that unfinished calculation.
