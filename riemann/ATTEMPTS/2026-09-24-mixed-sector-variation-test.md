# Mixed-sector summed variation — 2026-09-24

Gap and target: control the mixed-sector signed integral at o(exp(Φ_*)/a), uniformly on compact h-bands above 5. This could remove one remaining complement in the fixed-h assembly with L288; the small same-sign arguments, lower indices, and bounded heights still require their own checks. Test whether the sum of the L1 norms of mixed amplitude derivatives is o(exp(Φ_*)); two uniform oscillatory primitive bounds would then supply 1/a, including sector boundaries. Abandon this certificate if its index sum fails to decay.

Redundancy: L292 rules out a globally nonstationary phase, but not globally bounded primitives. L293 handles only cores. L275's rectangular identity and L282's global primitive mechanism suggest avoiding a delicate complement partition. L291 obstructs absolute mass at the required scale, not the use of two oscillatory primitives. Existing unfinished work is preserved.

Saved reasoning: with p=2n and H=n/r, the polynomial and its first two derivatives are bounded by C 2^(-p) exp(-p) exp(2H|u-v|). After summing theta indices, each differentiated exponential factor has a Gaussian-moment envelope C exp(5r-u/2) exp(-c exp(2(u-r))). Splitting exp(2H|u-v|) into two separable envelopes should give a total mixed variation C exp(Φ_*) exp((1/2-2H log 2)r). This decays for h>5. The proof must justify exact-factor derivatives, the infinite sum, and endpoint terms before claiming the estimate.

Result: [L294](../lemmas/L294-mixed-sector-summed-variation-bound.md) proves the summed mixed variation bound and uses two O(a^(-1/2)) primitives to obtain O(exp(Φ_*)exp(−γr)/a), γ=2(h₀−9/2)log 2−1/2>0. The exact-factor derivatives obey the same Gaussian-moment envelope. Absolute integral summability justifies the theta interchange; primitives anchored at zero account for the sector boundaries. Subtraction of L293 gives the requested complement bound with an additional harmless r² factor. This is ADVANCE toward the fixed-h complement gap, not a global sign extension or an RH candidate. Continue to the same-sign small-argument region because that independent region was not estimated here. Zero consecutive unresolved exploration turns.
