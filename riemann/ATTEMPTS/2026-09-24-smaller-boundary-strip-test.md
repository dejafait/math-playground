# Smaller below-five boundary-strip test

Gap and intermediate target: L320's logarithmic witness bound still needs the low-index Laguerre signs. Test the summed signed same-sign boundary integrals at h=39/8 with cutoff min(u,v)≤r/20 against o(exp(Φ_*)/a). Success would complement C294b and leave a larger retained arithmetic region for a later same-sign assembly at n~(3/16)log a. Its signed arithmetic margin, the other low indices and unbounded exterior heights would remain unresolved.

Redundancy and discriminating test: L295's fixed power-sum domination requires h>5; L301 treats only the endpoint; L323 disproves the r/4 cutoff at this h by actual stationary mass. None proves or disproves the smaller strip. Preserve their statements. Continue only if summing the exact Gaussian index envelopes yields a negative exponential rate for the polynomial, its first two derivatives and all rectangular boundaries. Stop this variation estimate if any required term fails the little-o threshold.

Saved unfinished reasoning: write w=u+v and P(w)=(w/(2r))^(3r/4). Summing j⁴(1+j²exp(2(u−r)))exp(−(h/2)j²exp(2(u−r))) should give a bound C exp(5(r−u)) with an additional Gaussian tail for u>r. This would bound the index sum of each required amplitude derivative by C exp(r−w/2) Σ_(ℓ≤2)|P^(ℓ)(w)|. On [0,r/20]×[0,101r/100], w/(2r)≤53/100, suggesting rate 47/100+(3/4)log(53/100)<−1/200. On the remaining rectangle, the retained Gaussian tail should dominate the coarse exp(r/4) bound. The lattice-sum estimate, derivative factors, parameterwise absolute integration, every edge and corner, and the reflected strip still require checking. This calculation asserts no signed arithmetic lower bound or Laguerre sign.

Completed result: [L324](../lemmas/L324-smaller-below-five-boundary-variation.md) proves the summed-modulus bound C exp(Φ_*)(1+r)²exp(−r/200)/a, with parameterwise absolute theta integration and both reflected full-integral estimates. Gaussian lattice summation gives the factor exp(r−w/2) and a retained tail exp(−(39/32)exp(2(v−r))). The first factor and exact polynomial derivatives supply exponential decay on the low rectangle; the tail handles the entire unbounded remainder. Two bounded oscillatory primitives give 1/a, with the corner, both edges and mixed derivative all estimated. Thus the achieved normalized error tends to zero, strictly meeting the required o(1) threshold.

Assessment: ADVANCE; zero consecutive unresolved exploration turns. L323's larger-strip obstruction remains valid: its stationary family is part of the interior under this cutoff. That interior's growing-index approximation and signed arithmetic margin remain unproved, so no Laguerre sign, zero-exclusion range or RH-candidate status changes. Continuing to that interior approximation is justified by the now controlled boundary and mixed sectors. The sole current action is in PROGRESS.md. Mathlib coverage is not checked. Analytic verification covers the Gaussian sum on both parameter ranges, all polynomial derivative orders, termwise integration and boundaries; exact rational arithmetic checks the cutoff identity, derivative monotonicity and strict rate below −1/200. All pre-existing changes are preserved.
