# Conditional cosine ordering test — 2026-09-24

Gap: the actual-theta Laguerre signs beyond height ten and the low-index signs at large heights remain unproved. The proposed intermediate target is monotonicity in s>0 of C_s(a)=∫q_s(t)cos(2at)dt/∫q_s(t)dt, where q_s(t)=k(s+t)k(s−t), initially for 10≤|a|≤20. If it holds, weighting by s^(2n) gives a covariance route to ordering the normalized Laguerre levels, potentially transferring a low-level sign. That low-level sign and any uniform all-height extension remain separate unresolved steps.

Discriminating test: with W=−log k and B_s(t)=W'(s+t)+W'(s−t), differentiation gives ∂_s C_s(a)=−Cov_(q_s/∫q_s)(cos(2at),B_s(t)). Seek a sign proof or a rigorously enclosed negative derivative at one rational pair (s,a) in the proposed band. A negative derivative stops this sufficient conditional-ordering mechanism; an inconclusive quadrature or failure of pointwise signing does not. Numerical screening alone will not be a certificate.

Redundancy and failure check: read the whole overview and global DAG, inspected the existing changes, and checked L267, L309, L311 and their assessments. L309 orders the variance because t² is monotone in |t|; cosine is not. L268 stops the quadratic certificate at growing heights, and the earlier generic masking examples do not decide this actual-theta conditional transform. No previous conditional cosine ordering test was found. All pre-existing work is preserved.

The unfinished reasoning was saved before calculation: put Z=∫q_s, N=∫q_s cos(2at), Z_s=∫∂_s q_s and N_s=∫∂_s q_s cos(2at). The exact sign numerator is N_s Z−N Z_s. Direct kernel derivative integrals avoid interval division by small pointwise kernel values. The calculation required separate bounds for quadrature error, omitted theta indices and the spatial tail.

The non-certified screen located a candidate ∂_s C_s(16)≈−2.2068×10^(−5) at s=1/5. The completed proof is [L312](../lemmas/L312-conditional-cosine-ordering-obstruction.md). It proves the required derivative domination and both infinite-tail bounds, splits the reflected finite head at t=s, and applies L036's eighth-derivative Taylor remainder on 256 panels. Trigonometric enclosures use polynomial remainders and L037's π bounds, without adding a sine/cosine arithmetic contract.

The [certificate](../scripts/laguerre/conditional-cosine-certificate.json) encloses the derivative strictly between −23/10^6 and −21/10^6 under L037's contracts. An independent exact-rational calculation from the four deliberately widened integral intervals in L312 also verifies these bounds. The required threshold was nonnegativity at every s in the proposed band; this actual-theta negative derivative violates it with a margin greater than 21/10^6.

**WHY IT FAILS.** Increasing curvature orders the nonoscillatory variance, but cosine reverses direction repeatedly. Its covariance with the conditional score is strictly positive at the certified pair, making the conditional cosine derivative negative. This is a counterexample to the proposed conditional ordering, not merely a failure of a sufficient pointwise estimate. It does not sign the covariance after averaging over s, so it neither disproves averaged level ordering nor supplies a negative Laguerre coefficient or an off-line zero.

Assessment: NEGATIVE; stop conditional cosine ordering as a transfer mechanism. The main gap and known |a|≤10 all-level sign range are unchanged; no RH candidate. Zero consecutive unresolved exploration turns. The reason for the subsequent weaker averaged comparison is L312's exact identity for R_(n+1)−R_n: the local counterexample does not decide that integral covariance, which could still transfer a low-level sign if controlled. General averaged ordering and the starting signs remain unproved.
