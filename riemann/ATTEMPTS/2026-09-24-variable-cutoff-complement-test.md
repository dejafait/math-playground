# Variable-cutoff complement test — 2026-09-24

Gap: the smaller logarithmic-index exterior signs required by L266 remain missing. Target: optimize the absolute complement of L288 using E_η={min(|s+x|,|s−x|)≤ηr}, with fixed small η>0. Its downstream use is a lower fixed-h global positivity band; sublogarithmic indices and bounded heights remain independent gaps. Continue if the complement is o(exp(Φ_*)/a); otherwise change the absolute-envelope mechanism. The actual loss to beat is a^6, not a^5.

Redundancy check: L279 and L284 discard the logarithmic polynomial penalty and split at |m−r|=r/4. L289 makes that coarse split explicit, giving h>96. L288's positive-sector proof uses a fixed cutoff but permits rechecking any fixed η>0. None of these retains the combined coordinate T=m+l in the complement estimate.

Saved working calculation: split at T=(1+ε)r. Above it the envelope has superexponential decay from exp(2(T−r)). Below it E_η forces m≤(1+ε+η)r/2. In the mixed sector the true polynomial is l^(2n); maximizing it together with exp(9m) under m+l≤(1+ε)r places l=m=(1+ε)r/2 for the h-range under test. As ε,η decrease to zero the decay coefficient approaches λ(h)=2(h−9/2)log 2+9/2. The required λ(h)>12 gives H=9/2+15/(4log 2), approximately 9.91, rather than 5. This working estimate is being checked before any global sign claim.

Result: [L290](../lemmas/L290-variable-cutoff-fixed-h-positivity.md) proves the global asymptotic on every compact h-interval above H, including a recheck of L288 at the changed cutoff. The achieved ratio is O(a^(6−λ/2)(log a)²), with λ>12, plus a superexponentially small term. ADVANCE extends the fixed-h sign range; it does not close the RH gap. The limiting inequality of this particular estimate remains strictly above h=5, so further cutoff tuning alone is not the next direction. Retaining the argument-dependent theta-series prefactors could reduce the coarse δ^(−5) loss and is the next discriminating test. Zero unresolved exploration turns; no RH candidate.
