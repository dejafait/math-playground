# Mixed-sign sector test — 2026-09-24

The main gap remains exterior Laguerre positivity through L266's witness cutoff. The intermediate target is o(exp(Φ_*)/a) for the mixed-sign sectors at log a≤h≤2 log a. Its downstream use is to supply the remaining sector estimate for the logarithmic-h assembly. The assembly, smaller indices, and bounded heights are distinct outstanding steps.

The checkpoint proposed deriving reflected theta-summand phases. Inspection of L269 reveals a simpler discriminating test: in these sectors the true polynomial factor is l^(2n), not m^(2n), for m=|x| and l=|s|. Outside L279's small-argument region and with |m−r|<r/4, l/m≤4/5. Continue if that retained factor absorbs δ^(−5) and the extra factor a required by the signed principal scale; otherwise return to reflected-phase cancellation. This is a new use of the existing envelope at logarithmic h, not a repetition of L269's small swapped-box bound at proportional indices. L279 alone controls only the small-argument region. L278's failure of absolute control concerns the same-sign exterior and does not preclude this test.

[L284](../lemmas/L284-mixed-sign-sector-suppression.md) proves an absolute bound Cδ^(−5)(r+1)²exp(Φ_*−c h r) on the entire mixed-sign region. Radial tails use the existing exponential envelope; in the central radial band the factor (4/5)^(2n) gives exp(−c h r), because n=r(h−9/2). Together with L279 this includes both mixed sectors and all theta indices without termwise phase manipulations. The relative bound has logarithm at most −c(log a)²/3+6log a+O(log log a), so it meets the actual little-o threshold rather than merely beating an absolute local scale.

Assessment: ADVANCE for the remaining sector estimate. Zero consecutive unresolved exploration turns. This step does not assert a new global sign range or an RH candidate. The reason for the next direction is that all sector estimates now appear available, but the assembly must check the actual-kernel principal patch against the first-summand patch and the exact reflection and partition identities before claiming positivity. Existing unfinished work is preserved.
