# Finite variance cutoff test — 2026-09-24

Gap: L309 gives all-level positivity only on |a|≤49/10. The intermediate target is a certified V_64<1/200. Its monotonicity would then prove every level n≥64 positive on |a|≤10, reducing this compact-height target to levels 1–63. Those lower signs, all larger heights, and the endpoint arithmetic margin would remain unresolved. Continue if a rigorous enclosure lies below 1/200; abandon this particular cutoff if a lower enclosure exceeds the threshold, or reassess the arithmetic method if its enclosure is inconclusive.

Redundancy and failure check: L267 supplies the moment formula and quadratic threshold; L268 blocks the growing-height use, not this fixed-height reduction; L309 supplies the strict ordering but only a non-certified V_64 probe. The saved Hankel moments stop far below degree 130. L039 is valid at that degree but its common factorial tail is too coarse to import without checking. Existing uncommitted changes are preserved.

The unfinished calculation was saved before the detailed work. The completed proof is [L310](../lemmas/L310-finite-variance-cutoff-at-height-ten.md). It replaces the coarse tail by an explicit common error below 9·10^(−23), using the actual powers and logarithmic slopes on the tail intervals. L036's midpoint Taylor panels and L037's interval implementation enclose all even half-line moments through degree 130. The factors of two cancel in the variance ratio, and the interval calculation propagates the subtraction P_64−Q_64 explicitly.

The [saved certificate](../scripts/laguerre/variance-cutoff-certificate.json) gives 0.0041187558<V_64<0.0041187800. In particular V_64<413/100000, below the required 1/200 by at least 87/100000. Monotonicity gives strict positivity for every n≥64 on |a|≤10, with normalized quadratic margin greater than 87/500. This is a finite reduction, not a certificate for levels 1–63 or an extension of the excluded interval for nonreal centers.

Assessment: ADVANCE, with zero consecutive unresolved exploration turns. The high-level compact-height target is established under the inherited arithmetic contracts. The next direction is finite lower-level signing because this cutoff removes the infinite tail of levels for |a|≤10; higher-order polynomial lower bounds for cosine retain information discarded by the failed level-one quadratic test. All larger heights and the endpoint arithmetic margin remain unresolved. No RH candidate is recorded.
