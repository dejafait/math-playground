# Actual-phase Poisson ratio-block test — 2026-09-24

Gap and intermediate target: the first averaged comparison at unbounded heights still lacks a one-sided arithmetic bound. Apply the stationary transform to the sharp actual-phase ratio block N/2<n,m≤N in L318, where X=sqrt(a/(2π)), N=floor(X) and L=log X. Retain all stationary terms and test whether their sign or a one-sided remainder is controlled at o(L^5), the scale needed below L317's positive (16/5)L^5 diagonal. A useful block estimate could contribute to that comparison; the other blocks, product terms, first-level positivity, higher comparisons and the complete exterior witness target would remain unresolved. Continue if the transformed block has an independently controlled lower bound at that scale; stop this direct transformation as a signing mechanism if it reproduces an unsigned form with no smaller bound.

Redundancy review: read GOAL.md, PROGRESS.md, the whole PROOF.md overview and global DAG, and inspected the tracked changes and untracked inventory. Preserve all existing work. L317 already stops absolute and third-derivative dominance; L318 tests multiplicative phases without imposing the actual twist-cutoff relation. L175 and L178 already show that a stationary transform need not improve a different heat-route first-moment problem. The present test must therefore assess the actual AFE polynomial ratio kernel and its quadratic error, not count the generic existence of a dual sum as progress. Zero consecutive unresolved exploration turns precede the test.

Unfinished reasoning saved before completing the estimate: for W_j=Σ_(N/2<n≤N)n^(−1/2)(θ'(a)−log n)^j exp(−ia log n), j≤4, the stationary point at positive dual frequency k is x_k=X^2/k. The transformed amplitude appears to be k^(−1/2)(log(k/X)+δ)^j, δ=θ'(a)−log X, on X^2/N≤k<2X^2/N. Its common phase is exp(i[a−a log(X^2)+π/4]). The standard B-process should give individual error O(N^(−1/2)log N), hence quadratic ratio-form error O(log N)=o(L^5). All endpoint conventions, uniformity and the exact reflected polynomial kernel still need checking. The expectation is a reciprocal-annulus form rather than a positive representation; no actual sign, sign-range extension or RH candidate is asserted at this checkpoint.

Completed result: [L319](../lemmas/L319-actual-phase-ratio-block-stationary-involution.md) proves the uniform transform including all cutoff crossings. Its full ratio form and its off-diagonal part each equal the corresponding exterior reciprocal-interval form up to O(log a). The diagonal is bounded, the off-diagonal absolute coefficient budget remains of order sqrt(a), and the third-derivative bound on this single block gives O(a^(1/3)). The stationary remainder is below L^5, but the retained one-sided envelope is above it. The normalized one-sided o(L^5) target is equivalent on the two intervals. This uses the actual relation a=2πX^2, which was absent from L318's test twist.

WHY IT FAILS: the stationary map preserves the critical-line coefficient weight, reverses the logarithmic frequencies, and cancels its common phase inside the ratio form. Evenness of P_κ restores the same polynomial kernel, up to a summably negligible δ correction. Applying the transform again returns the starting quantity. Thus the small stationary error does not sign the retained main sum; the [canonical proof](../lemmas/L319-actual-phase-ratio-block-stationary-involution.md) transfers the missing lower bound rather than proves it. The power-sized envelopes do not assert actual negative values, and no failure of actual ordering or RH follows.

Assessment: NEGATIVE, zero consecutive unresolved exploration turns, no RH candidate, and no change to established sign/exclusion ranges. Stop direct repeated block transformation as a signing mechanism. L175 and L178 were prior warnings, not inputs or substitutes for this AFE kernel calculation. The distinct circle-growth direction is motivated by L266's still height-independent maximum bound and its height-dependent off-center anchor; cancellation of their common gamma decay could reduce the witness count, while required low-index signs would remain unresolved. All pre-existing work is preserved. Verification is analytic: the cited B-process hypotheses and endpoints, both stationary phases, coefficient variation, quadratic error transfer, diagonal substitution and third-derivative scales are checked in the proof. Mathlib coverage is not checked; no numerical sign computation is used.
