# Finite Laguerre multiplicity masking test — 2026-09-23

The gap is global all-level positivity or another exclusion of off-line zeros; the main mixed reciprocal-node forms remain unproved. The intermediate target was to decide whether adding real-root factors can make both D₁ and D₂ nonnegative everywhere despite a retained nonreal pair. This tests the downstream value of pursuing the second-level theta sign alone. An exact masking example would stop the generic sufficiency inference; a proved obstruction to masking could motivate a zero-exclusion lemma. Neither outcome would itself settle actual theta positivity.

The complete overview, global DAG, checkpoint and existing changes were read and preserved. The redundancy screen checked L251–L252's first-level masking and its second-level detection, L255–L256's compact signs and cross-term obstruction, and the finite-test discussion in L057. The latter concerns finitely many Hankel matrices, not finitely many Laguerre levels at every real center. None already supplies this global finite-level construction.

[L257](../lemmas/L257-real-root-multiplicity-masks-finite-laguerre-levels.md) supplies an exact affirmative answer with J(z)=z³(z²+b²), and the same binomial calculation extends it to any prescribed finite K by using z^(2K−1)(z²+b²). An even translated product also satisfies the recorded coarse zero localization. The identities are polynomial identities valid even at the repeated real roots, so no singular normalization or numerical sampling is used. The explicit negative coefficient D_(2K)(J;0)=−2b² shows where this local example eventually fails.

WHY IT FAILS: Any fixed finite list of global nonnegative Laguerre inequalities can be masked by real-root multiplicity. Thus second-level positivity alone cannot serve as a generic completion mechanism, even with symmetry and the coarse strip localization. This does not negate its possible value with additional actual-theta input; the counterexample neither has a positive Fourier kernel nor proves strict signs or a simple-root version. The required all-degree positivity remains entirely missing outside the compact range. No RH candidate is obtained.

Decision: NEGATIVE; stop finite-level sufficiency as a generic bridge. One bounded test resolved, zero consecutive unresolved exploration turns. The next direction is quantitative all-level detection: seek an explicit upper bound on a negative-coefficient witness index from a hypothetical nonreal zero and Cauchy bounds, rather than increasing a fixed finite list. Such a bound could identify a height-dependent target, but obtaining actual-theta positivity through that target would remain unresolved. No library lookup or computational experiment was needed for the exact binomial proof.
