# Exact-summand low-h test — 2026-09-24

Gap: the exterior Laguerre signs below L286's logarithmic-h threshold. Target: a positive global asymptotic for a→∞, h→∞, h≤log a. Downstream use: reduce the unresolved indices toward L266's all-index witness requirement; bounded h and bounded exterior heights remain separate gaps.

Discriminating test: all errors must be o(exp(Φ_*)/a). Retain the exact first-summand factor E in the local Fresnel integral, and sum higher theta indices at their own stationary points using the envelope from L282. Continue if the local correction gains the oscillatory 1/a factor and the global errors remain small; abandon this extension if a surviving error requires h comparable to log a.

Redundancy/failure check: L278 and L286 transfer the entire kernel locally using a modulus error a exp(−γh)/h, which fails in this range. L283 already retains exact factors and bounds higher-index patches, but states only logarithmic-h hypotheses. The proposed change uses the exact first summand as the main term, without transferring the full kernel on its square. L278's absolute exterior obstruction remains valid evidence; no whole-exterior absolute estimate is proposed.

Working checkpoint: on the fixed principal square, E−1 and its first and mixed derivatives are O(1/a). Multiplying by L278's amplitude with uniformly bounded rectangular variation should give a signed correction O(1/a²). The explicit sector errors are h²/√a and h²2^(−h); the small-argument/mixed-sector error is at most C a^6 h^(−5)(r+1)²exp(−c h r). All tend to zero if h→∞ and h≤log a. The remaining check is to extend each bound's proof, rather than invoke a narrower statement outside its hypotheses.


Result: [L287](../lemmas/L287-exact-summand-diverging-h-positivity.md) proves the extension. The exact principal-factor correction has bounded rectangular variation O(1/a), giving a signed O(1/a²) error. Higher-index clipped squares remain relatively O(h²2^(−h)), nonstationary complements O(h²/√a), and the other sectors O(a^6 h^(−5)(r+1)²exp(−chr)); all vanish for h→∞, h≤log a. These are checks of the earlier proofs on the extended parameter domain, not applications of their narrower conclusions. Combined with L286, positivity covers n/log a→∞ through any fixed linear cutoff. This is ADVANCE, with zero consecutive unresolved exploration turns. It still does not cover L266's indices starting at 1 or bounded exterior heights. The next direction retains the full higher-index leading sum at fixed h, because bounding that sum as a negligible error ceases to give an asymptotic there. No RH candidate is recorded.
