# Logarithmic-band assembly test — 2026-09-24

The main gap is actual-theta exterior Laguerre positivity through L266's height-only witness cutoff. This step targets the global positive saddle asymptotic for log a≤h≤2 log a. Its downstream use is a new low-index sign band; smaller indices, interpolation to the previously established high-index range, and bounded exterior heights remain separate unresolved steps.

The discriminating test is whether the positive-sector sum, the reflected sector, and the remaining regions partition the whole plane, with every discarded term o(exp(Φ_*)/a), and whether L278's actual-kernel patch can be replaced by L283's exact first-summand patch at that precision. Continue if both checks pass; abandon an unconditional assembly if either leaves a main-scale remainder. L278 alone is only local, L283 assembles only the positive sector, and L284 only suppresses mixed sectors, so none duplicates this target. L278's absolute exterior obstruction is retained: the positive sector must use L282's signed estimate. The historical portfolio stop imposes no extra admission condition.

Working checkpoint: split into E, D, its s-reflection, and M outside E. On the principal square compare both actual theta and T_1 to k_0; their modulus-error integral is bounded by exp(Φ_*)[a^(−1)+exp(−γh)]/h. Reflection in s swaps the two kernel factors exactly, leaving the Fourier factor unchanged. These checks give the required normalization without introducing a second summation over overlapping patches.

Result: [L285](../lemmas/L285-logarithmic-band-global-laguerre-positivity.md) proves I_n(a)=(8π²)²exp(Φ_*)[π/a+o(1/a)] uniformly in this band. All partition and normalization checks pass. The relative patch correction is O(1/h+a exp(−γh)/h); the signed positive-sector remainders are O(h²2^(−h)+h²/√a); the other sectors have exp(−c h r) decay absorbing the strip loss. These meet the actual little-o threshold. ADVANCE supplies a new global sign band, not the full witness range. Zero consecutive unresolved exploration turns. The next direction tests interpolation because the new band remains separated from L277, and no monotonicity of Laguerre signs across the gap has been proved.
