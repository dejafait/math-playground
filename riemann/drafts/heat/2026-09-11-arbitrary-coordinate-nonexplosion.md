# Arbitrary-coordinate nonexplosion — 2026-09-11

Draft checkpoint, initially unproved. Apply L122 to obtain positive
increasing unbounded f with forward generator bounded by B. For R>n,
collapse destinations j>=R to one absorbing state with payoff f(R),
retaining their complete rate sum. The finite-state expectation identity
should give P(tau_R<=t)<=(f(n)+Bt)/f(R). On the original embedded-chain
space, the exit jump counts tend to infinity and tau_R increases to T.
Then nonexplosion follows and L107 gives height-dependent selection.

Resume at the cutoff audit: verify finite rates despite small gaps,
exit coupling, payoff cap direction, and convergence of stopped sums.
No assertion about uniformity in heights or heat parameters is intended.

Completed audit: Lemma 123 now contains the proved result. Full exit
rates are retained, payoff capping decreases the generator, and the
stopped holding-time sums converge pathwise to T. This draft records
the intermediate checkpoint; the lemma is authoritative.
