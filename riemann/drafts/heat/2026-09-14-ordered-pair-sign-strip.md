# Ordered-pair sign strip checkpoint

Prior L194 is complete. Expand Y_N using the ordered pairs of L186.
For the actual profiles L178–L179, q(x)/p(x)=-tanh((3/2)log(x/sqrt(2))).
Writing A=(3/2)log(cd/(2N²)), D=(3/2)log(c/d), the product
of these ratios equals (cosh A-cosh D)/(cosh A+cosh D).
Draft target: in |cd-2N²|<=K N^(3/2), its positive part is O_K(N^-1).
Combine this with the absolute pair-mass bound from L193 and positive
alpha from L194 to bound the ordered positive mass by
O_epsilon(h N^(1/2+epsilon))=o(Nh). This does not control the negative
mass or the complement. Resume by checking the exact identity and all
nonnegative weights, then store the scoped lemma and validation.

Completed in L195: the identity, nonnegative ordered weight expansion,
and central-strip positive-mass bound are proved analytically. The
remaining negative strip mass and exterior signed mass are unproved.
No failed asymptotic estimate was promoted to a proved input.
