# Square-product stationary contribution — 2026-09-13

Checkpoint: unproved until the bounds below are checked. L182 is complete;
existing work is preserved. For k=m², the exact saddle phase is i, so
Re(i F(z−,z+))=∫_(z−)^(z+) sin(z²) dz. Do not discard this projection
or replace the finite endpoints by infinity.

Proposed scope: bound the square-product subset absolutely by
O_epsilon(N^(−1+epsilon)), using O(h) possible integers m and the
ordered factorization bound d_4(m²)=O_epsilon(N^epsilon). Prove the
latter directly by prime factorization, then bound finite Fresnel
integrals uniformly. The subset error should be O_epsilon(N^(−2+epsilon)).
Resume with these checks and record only the square-subset conclusion;
no estimate for nonsquare products or the complete mixed moment follows.

Completed: L183 proves both proposed bounds, using a prime-factorization
proof of the divisor estimate. The exact real projection is retained.
No nonsquare estimate or full stationary cancellation is claimed.
