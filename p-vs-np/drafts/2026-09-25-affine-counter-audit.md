# Archived affine counter audit sketch — 2026-09-25

The shared rules, local goal, current checkpoint, entire overview and DAG, existing changes, relevant trace and padding proofs, and prior failure notes were inspected. The Clay page and Cook's official statement were rechecked. Existing work is preserved. The queued affine-counter test has no existing proof in this notebook; the earlier results concern different certificate mechanisms.

**Gap and intermediate target.** The diagonal route still lacks certificates of one fixed polynomial size with a sound polynomial-time verifier for the required computations. Test an exact affine lift of the cyclic w-bit counter over F₂, with affine decoding of every counter bit. A polynomial-dimensional lift would permit evaluation of long blocks by repeated squaring, provided the representation can be built and validated efficiently. Coverage of general computations, certificate soundness and completeness, and same-input diagonal disagreement would remain separate unresolved steps.

**Discriminating test.** Let φ send counter states to F₂^d, require φ(c+1 mod 2^w)=Aφ(c)+a, and require affine recovery of all w binary digits. Seek d bounded by one fixed polynomial in w. An exponential lower bound, even with completely arbitrary encoding φ, stops this representation as a universal compression mechanism. The test concerns this exact F₂ model, not arbitrary compressed certificates or circuit size.

**Completion.** The difference-chain argument and binomial-coordinate construction saved before detailed work are fully proved in [L004](../lemmas/L004-exact-affine-counter-dimension.md). The exact dimension is 2^(w−1); the proof handles cyclic wrap, affine constants, independence, all decoded bits, and w=1. The polynomial-in-width requirement fails for explicitly stored coordinates, while arbitrary implicit representations and efficient ordinary counting remain outside the obstruction. The finite sanity checks also passed. This archive carries no unfinished proof or current action; no full resolution candidate is asserted.
