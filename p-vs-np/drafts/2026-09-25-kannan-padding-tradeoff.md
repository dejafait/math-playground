# 2026-09-25 — Padding the constructive fixed-exponent witnesses

Saved before completing the quantitative argument. The current checkpoint remains in PROGRESS.md.

**Gap and intermediate target.** L008 gives a tagged join with a circuit lower bound but no common NP verifier. Test whether padding Cai–Watanabe's constructive witnesses can supply a common resource bound while preserving unbounded circuit lower-bound exponents. If both survive, the joined language could be useful for a separation, subject to the still-unresolved removal of its universal stage. If the exponent transferred by hardwiring is bounded after resource normalization, stop this particular padding repair.

**Redundancy check.** L008 and ATTEMPTS/007 isolate quantifiers and the unpadded join; they do not quantify the cost of this padding. L003 and ATTEMPTS/003 concern explicit deterministic transcripts and same-input diagonalization, a different mechanism. Existing work is preserved. The Clay statement and Cook's official description have been reconfirmed for the ordinary P-versus-NP target.

**Source inspected.** Cai–Watanabe, [report C-256, Theorem 1 and §2](https://www.is.c.titech.ac.jp/report-c/C-256.pdf), states a constructive Sigma_2 machine with running time O_k(n^(k^2) (log n)^(k+1)) and an n^k circuit lower bound. Their circuit size counts wires. Pin the source's exact bound, account for conversion to this notebook's bounded-fan-in gate count, and avoid strengthening a unit-coefficient lower bound without justification.

**Argument to complete.** For a fixed tag k, let padded length be N_k(n), with the original n bits left as free input coordinates. Restricting a circuit for the padded join to this slice costs at most three gates for constants, so c_A_k(n) <= c_J(N_k(n))+3. If N_k(n) is n^(a_k) up to fixed-k logarithmic factors, the lower-bound exponent available from this restriction is k/a_k. Normalizing the advertised n^(k^2) resource budget into N^C, with one fixed C, requires a_k C >= k^2. Thus k/a_k <= C/k, which is bounded. This is a limitation of the given bound and restriction argument, not a lower bound on all algorithms or proof that the padded language has small circuits. Padding preserves the existential/universal alternation unless a separate argument removes it.

**Open details for this step.** Give a parseable padded encoding, state the general restriction result precisely, control constants and logarithms, distinguish a sufficient advertised time budget from an intrinsic time lower bound, and decide whether the supplied quantitative data meet the unbounded-exponent test. No resolution candidate has appeared.

**Completion.** [L009](../lemmas/L009-padded-join-exponent-tradeoff.md) supplies the complete clocked-join construction and exponent comparison. The source's exact alternating bound and circuit-size convention are recorded in the existing source note. The fixed-tag threshold is bounded after normalization; the explicit clocked join inherits exponent 1/j, with its logarithmic loss retained. This archive contains no unfinished proof or current next action. The route decision and sole current action are in PROGRESS.md.
