# 2026-09-25 — Exact affine counter dimension

Step identifier: 2026-09-25-04-affine-counter-dimension. Outcome: NEGATIVE.

The shared rules, local goal, checkpoint, entire overview and DAG were read before selecting work. Existing changes, the relevant trace and padding proofs, prior failure notes, and saved drafts were inspected and preserved. No earlier result supplied this affine-dimension bound. The [Clay page](https://www.claymath.org/millennium/p-vs-np/) and [Cook's official statement, §1](https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf) were rechecked; the existing 2026-09-25 source reconfirmation remains accurate. Other notebooks and shared infrastructure were not modified.

**Gap, target, and test.** The selected action tested whether exact F₂-affine evolution with affine bit decoding could represent a cyclic w-bit counter in polynomial dimension. Such a representation would support repeated-squaring evaluation of long blocks and could be a component of structured computation certificates. Efficient construction and validation, coverage of general computations, and same-input diagonal disagreement remained unresolved. An exponential dimension lower bound would stop this particular representation; a polynomial construction would justify testing its coverage and verifier obligations. The earlier rank, arbitrary-descriptor, and padding failures did not answer this test.

**Result and decision.** [L004](../lemmas/L004-exact-affine-counter-dimension.md) proves the exact minimum dimension 2^(w−1), using a shift-invariant function space for the lower bound and binomial coordinates for attainment. The affine constant, cyclic wrap, arbitrary encoding, and w=1 boundary are included. The [attempt note](../ATTEMPTS/004-exact-affine-counter-compression.md) records the local stop, and the [saved sketch](../drafts/2026-09-25-affine-counter-audit.md) is archived as complete. This is new negative evidence against the proposed representation, not a P-versus-NP advance or a candidate resolution. Exploration turns without an advance or informative negative remain 0.

**Required versus achieved bound.** The desired dimension was at most C(w+1)^k for fixed C,k. The exact minimum is exponential in w and equals half the full cycle length. The proof separately compares this with the actual NP requirement, polynomial certificate length in full input length n: fixed w=O(log n) can still give polynomial dimension, whereas full-cycle widths proportional to an arbitrary clock exponent times log n cannot give a uniform exponent by this representation. This conditional comparison is not a bound on all diagonal-language certificates. It also does not lower-bound the size of a succinct formula describing exponentially many coordinates. Ordinary modular addition still evaluates counter endpoints efficiently.

**Effect on the gap and reason for the subsequent direction.** No NP language outside P has been obtained. This certificate-format experiment is complete, while membership of the diagonal language D_full in NP remains unproved. An intrinsic complexity classification of D_full would clarify what every possible certificate family would need to achieve, instead of diagnosing only one representation at a time. The EXPTIME-completeness test selected in PROGRESS.md has not been carried out in this step; its unary exponent and input-length accounting require proof.

**Review and verification.** The proof was checked for shift invariance without an invertible matrix, affine rather than linear decoding, independence of the difference chain, the characteristic-two power identity, and decoding all bits in the attaining construction. No existing local lemma is a mathematical input, so the added DAG row has an empty right side. Mathlib coverage is not checked. `python3 scripts/affine-counter/check_small_cases.py` passed every transition, wrap, decoded-bit, and rank check for widths 1–8; [the report](../scripts/affine-counter/small-cases.txt) records the results. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with four nodes and no edges; the environment flag prevents bytecode writes to shared infrastructure. `git diff --check -- .` also passed. These checks establish documentation consistency and finite sanity only, not the all-width theorem or a complexity separation.
