# 2026-09-25 — Constructive-witness padding loses the exponent

Step identifier: 2026-09-25-09-kannan-padding-exponent-tradeoff. Outcome: NEGATIVE.

Read the shared rules, local goal and checkpoint, whole overview and DAG, and existing changes before selecting the recorded padding test. The relevant prior result and attempts were inspected; their unfinished and completed artifacts were preserved. The [Clay problem page](https://www.claymath.org/millennium/p-vs-np/) and [Cook's official description](https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf) still give the ordinary P-versus-NP target. No other notebook or shared infrastructure was edited.

**Gap, target, and test.** The remaining aggregation gap was one NP language retaining unbounded circuit lower-bound exponents. The intermediate target was a padded combined predicate with one common resource bound and unbounded ratios of hardness exponent to padding exponent. Such a result could support separation, with removal of the universal stage still unresolved. A bounded ratio would stop this proposed repair. L008 already handled the unpadded join; L003's explicit-transcript diagonalization is a different padding obstruction, so this calculation was not a repeat of either result.

**Result.** The [updated source note](../foundations/03-kannan-fixed-exponent-theorem.md) pins Cai–Watanabe's Theorem 1, its alternating time bound, and its wire-count convention. [L009](../lemmas/L009-padded-join-exponent-tradeoff.md) completes the [saved calculation](../drafts/2026-09-25-kannan-padding-tradeoff.md). A clocked padded join has one uniform Sigma_2^P bound even when parameter-dependent machine construction is expensive. Its known lower-bound exponents become 1/j. More generally, covering the given resource budget within one N^C bound makes the fixed-tag transfer threshold at most C/j. The [attempt note](../ATTEMPTS/008-normalizing-kannan-witnesses-by-padding.md) stops that repair. This is new quantitative evidence affecting the route, not a repeated stop review or a separation result. Exploration turns without an advance or informative negative remain 0.

**Required versus achieved.** The circuit route needs exclusion of every fixed polynomial exponent for one NP language. The unpadded join retains hardness but lacks the required membership. The explicit padded join supplies a common alternating bound, while the inherited exponents are at most 1/2; the general normalized comparison remains bounded for every fixed resource exponent C. The source's time upper bound is not an intrinsic computational lower bound, and failure of this transfer is not an upper bound on the joined language. No complete candidate appeared; status remains IN_PROGRESS.

**Effect and reason for the subsequent direction.** This closes the outstanding quantitative padding test. Both quantifier-only aggregation and this proposed normalization fail to establish membership and hardness together. The subsequent direction changes mechanism to proof complexity: testing resolution length against linear-algebra decision on Tseitin formulas will discriminate whether a restricted proof lower bound can constrain the unrestricted SAT computation required by the main goal. The concrete action is recorded only in PROGRESS.md.

**Review and validation.** Reviewed the parseable full-length encoding, cutoff for machine construction, fixed-j constants, existential/universal semantics, unbounded hard lengths, three-gate restriction cost, logarithmic factors, unit-coefficient versus O-size lower bounds, and the distinction between published resource budgets and actual minimum time. L009 uses L008's restriction and collapse arguments; its new DAG row records that direct input. Mathlib coverage is not checked. The argument requires no numerical experiment. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with nine nodes and four edges. `git diff --check -- .` also passed. These checks establish documentation consistency, not mathematical correctness.
