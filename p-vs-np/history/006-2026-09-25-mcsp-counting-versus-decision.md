# 2026-09-25 — Circuit counting versus MCSP decision hardness

Step identifier: 2026-09-25-06-mcsp-counting-versus-decision. Outcome: NEGATIVE.

The shared rules, local goal, checkpoint, whole proof overview and DAG were read before choosing work. Existing tracked changes, prior failed approaches, and the preceding classification assessment were inspected and preserved. No earlier result tested circuit counting for MCSP. The [Clay page](https://www.claymath.org/millennium/p-vs-np/) and [Cook's official statement, §1](https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf) were rechecked; the existing 2026-09-25 foundation audit remains accurate. The [Kabanets–Cai author page](https://www.cs.sfu.ca/~kabanets/Research/circuit.html) supports the MCSP definition, with no external hardness theorem imported.

**Gap, target, and test.** MCSP's direct circuit certificate avoids the earlier missing NP-membership obligation, but the main missing step is still unrestricted decision hardness. The intermediate target was to compare circuit-description counts with full truth-table input length, and test whether an unbounded threshold range could have both exact polynomial-time recognition and almost universal rejection. Such a range would reject density alone as the proposed transfer, while leaving room for mechanisms using more structure. This differs from the balanced-rank and trace-certificate failures; those lemmas are historical contrasts, not mathematical premises.

**Result and decision.** [L006](../lemmas/L006-mcsp-counting-versus-decision.md) proves the counting bound and exact low-threshold algorithm, completing the [saved sketch](../drafts/2026-09-25-mcsp-counting-audit.md). The failure test succeeds: an increasing threshold of order log N/log log N permits polynomial-time exact MCSP recognition with a NO fraction at least 1-N^3/2^N. This is new evidence against the bare counting-to-hardness inference, preserved in the [attempt note](../ATTEMPTS/005-mcsp-counting-to-decision-hardness.md). It does not give a decider for general MCSP_u or exclude its hardness. No complete P-versus-NP candidate appears. Exploration turns without an advance or informative negative remain 0.

**Required versus achieved bounds.** At the high threshold floor(N/(32 log N)), counting gives represented-function complexity Omega(N/log N) for all but a 2^(-3N/4) fraction of N-bit tables. Every represented function also has an O(N log N)-gate DNF construction. These are polynomial scales in the full MCSP length M=Theta(N) at these thresholds. Neither concerns the circuit size of the MCSP predicate, for which superpolynomial unrestricted size would suffice, or rules out every polynomial-time decider. The exact enumeration algorithm applies only to the separate low-threshold range. The constant answer NO at the high threshold has tiny uniform-distribution error but remains incorrect on YES instances.

**Effect on the gap and reason for the subsequent direction.** MCSP_u is an NP-defined target, but no lower bound for its exact decision predicate has been obtained. A distinguishing reduction on structured tables offers a concrete way to examine how recognition could interact with an actual computational assumption, rather than inferring hardness from cardinality. Restricting a keyed function to logarithmically many input bits is the candidate mechanism for keeping truth-table construction polynomial in key length. Its security premise, circuit-size accounting, and distinguishing advantage are not established in this step; a conditional consequence would not by itself be an unconditional P-versus-NP separation. The one concrete test is recorded in PROGRESS.md.

**Review and verification.** The proof was reviewed for the unary encoding, malformed inputs, NP certificate length, topological enumeration, NOT dummy slots, free-constant conventions, output wires, integer rounding, r=1, polynomial uniformity, and the two distinct input-length scales. L006 has no earlier lemma as a mathematical input, so its DAG row is empty; all old nodes and edges remain. Mathlib coverage is not checked. `python3 scripts/mcsp-counting/check_bounds.py` passed 400 low-threshold checks for r=1,...,256 and 249 high-threshold checks for r=8,...,256. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with six nodes and one edge; the environment flag prevents bytecode writes to shared infrastructure. `git diff --check -- .` also passed. These arithmetic and documentation checks do not verify a complexity separation.
