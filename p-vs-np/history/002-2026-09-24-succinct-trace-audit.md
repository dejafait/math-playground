# 2026-09-24 — Supplied succinct trace validity

Step identifier: 2026-09-24-02-succinct-trace-validity. Outcome: NEGATIVE.

The shared and local instructions, whole argument overview, and DAG were read before selecting work. Existing modified root documents, L001, the rank failure note, the prior history, and the archived draft were inspected and preserved. There was no unfinished local proof to resume or previous trace-validity result to duplicate. Changes in other notebooks were left untouched. The current Clay page and official description were rechecked against the existing target audit; the target and conventions remain as recorded in foundations/.

**Gap, target, and test.** The selected step was the preceding checkpoint's proposed test of succinct computation verification. Its plausible downstream use was to place a clocked diagonal construction within NP using a fixed polynomial verification bound. The sought intermediate result was a polynomial-time validator, or a precise obstruction to one, for a supplied circuit mapping binary times to configurations. The discriminating test was a tautology reduction surviving the initial/final state and transition constraints. Certificate existence for the required long computations and diagonalization against every polynomial exponent remained explicitly unresolved. This was a different mechanism from the stopped rank inference, not a relabeling or repetition of that failure.

**Result and decision.** [L002](../lemmas/L002-succinct-trace-validity.md) proves coNP-completeness of the supplied-description validity language. The construction uses a fixed ordinary initial state, a fixed machine accepting after one step, stationary padding of halting configurations, and a circuit whose deviations from the valid run encode falsifying assignments. It preserves correct endpoints and legal configurations. Both the deterministic-validator consequence P=NP and the NP-certificate consequence NP=coNP are proved conditionally and kept distinct. A small descriptor can contain only two illegal transitions, giving a precise bound against uniform local sampling. These results meet the preselected failure test and change the route decision: generic succinctness is not an established polynomial-time verification method. The failed shortcut is preserved in [the attempt note](../ATTEMPTS/002-generic-succinct-trace-verifier.md); no novelty claim is made.

**Comparison with the required bound.** The required bound is N^d for one fixed d in full description length N. The horizon has only O(m) binary bits but its value is 2^(m+2)−1. Checking all transitions takes O(T·poly(N)) by the direct algorithm, and the sparse-defect family has N=O(m log(m+2)), so polynomially many uniform samples have vanishing detection probability. Neither observation alone is a time lower bound. The stronger statement actually established is the completeness reduction, which still proves no separation. It does not rule out selecting a structured subset of descriptions: this very fixed computation has a simple valid canonical descriptor.

**Effect on the main gap and reason for the subsequent direction.** No fixed NP language outside P, polynomial-time SAT algorithm, or candidate resolution has appeared. The result locates a universal validity quantifier hidden in a proposed short certificate. A different repair worth testing is explicit padding, which avoids arbitrary succinct-trace semantics but must still satisfy one fixed verifier exponent when the simulated decider receives the full padded input. The historical generic-validator stop leaves restricted certificate families open; it is not a global stop on research. PROGRESS.md holds the single concrete action. This is an informative negative result, so consecutive exploration turns without an advance or informative negative remain 0.

**Review and verification.** The reduction was checked in both directions, including the first transition, correct final state, strict interior address interval, fixed configuration width, total treatment of malformed strings, and polynomial construction length. The malformed-input fallback was refined during review to preserve the correct-endpoint guarantees as well. The coNP upper bound uses a single bad address, not an exponentially long certificate. The NP=coNP implication concerns certificates for supplied-descriptor validity and is not extended to existence of some other valid descriptor. No computational experiment was needed for the symbolic reduction. L002 uses the standard Cook–Levin input but no earlier local lemma, so its empty DAG row is intentional and L001 is unchanged. Mathlib coverage is not checked. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with two nodes and no edges; the environment flag prevents bytecode writes to shared infrastructure. `git diff --check -- .` also passed. Structural validation does not verify the mathematical argument.
