# 2026-09-25 — Resolution length separates from parity decision time

Step identifier: 2026-09-25-10-tseitin-resolution-versus-decision. Outcome: NEGATIVE.

Read the shared rules and prompt, local goal and checkpoint, whole overview and DAG, and existing changes before selecting the recorded Tseitin test. Inspected the relevant prior proof and failure notes; preserved all earlier work. The [Clay page](https://www.claymath.org/millennium/p-vs-np/) and [Cook's official statement, §1](https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf) still identify ordinary uniform P versus NP as the target. Edits stayed within this notebook.

**Gap, intermediate target, and test.** The missing passage was from restricted proof size to the running time of every SAT decider. A bounded-degree parity family with exponential resolution size but polynomial decision would falsify the direct passage and identify what a stronger model must address. Continue that inference only if its computation-to-proof simulation survives the test; abandon it if an ordinary-CNF recognizer and efficient decision procedure coexist with the lower bound. L001 tests a different statistic, balanced rank; no earlier artifact establishes the resolution discriminator.

**Result and decision.** [L010](../lemmas/L010-tseitin-resolution-versus-decision.md) completes the [saved construction](../drafts/2026-09-25-tseitin-resolution-test.md), using the [pinned width theorems](../foundations/04-resolution-width-theorems.md). A cycle plus twelve permutations yields connected degree-26 graphs with balanced-cut size at least n/12 by a union bound below (8/9)^n. The CNFs have exponential resolution length for odd charges, while one total SAT decider recovers parity equations and runs in polynomial time on every family member. The [failure note](../ATTEMPTS/009-resolution-length-to-decision-time.md) stops the direct instancewise transfer. This is new negative evidence for this notebook, not a novel theorem claim, repeated stop review, or separation. Exploration turns without an advance or informative negative remain 0.

**Required versus achieved.** The main target requires excluding every polynomial-time SAT decider or supplying one. The achieved bound excludes short resolution refutations of these CNFs, with no extension axioms, and is 2^(Ω(N/log N)) in binary input length N rather than 2^(Ω(N)). It coexists with polynomial decision on the same class, including satisfiable inputs. The fallback decider is not claimed polynomial on all SAT. No complete candidate appeared; status remains IN_PROGRESS.

**Effect and reason for the subsequent direction.** Proof length alone does not close the model-transfer gap. Merely choosing a stronger system would leave that gap unspecified; the relevant issue is whether it can simulate the certificates attached to an arbitrary efficient decider without assuming short proofs of the decider's correctness. This motivates the single concrete action in PROGRESS.md and does not reinstate earlier stopped mechanisms.

**Review and validation.** Checked the falling-factorial probability, deletion of crossing-edge endpoints, connectedness and bounded degree after merging edges, distinct vertex supports, both charge parities, full binary input length, the exact theorem scope, the n≥624 width-gap threshold, and the distinction between an instancewise simulation and a global conditional implication. The finite script passed 252 local valuations, all 232 charge vectors on 13 selected small graphs, and 256 clause-subset tests; [output](../scripts/tseitin-resolution/small-cases.txt) records the limited scope. L010 uses no earlier local lemma as a mathematical input, so its DAG row is empty. Mathlib coverage is not checked. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with 10 nodes and four edges, and `git diff --check -- .` passed. These checks validate the documentation and finite encoding cases, not a proof of the main target.
