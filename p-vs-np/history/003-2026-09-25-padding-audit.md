# 2026-09-25 — Unary padding and the same-input requirement

Step identifier: 2026-09-25-03-unary-padding-diagonalization. Outcome: NEGATIVE.

The shared rules, local goal, checkpoint, entire proof overview and DAG were read before selecting work. Existing tracked changes, the previous trace argument and failure notes were inspected and preserved. The prior draft was completed rather than unfinished; no padding result was already present. Other notebooks were left untouched. The [Clay page](https://www.claymath.org/millennium/p-vs-np/) and [Cook's official statement, §1](https://www.claymath.org/wp-content/uploads/2022/06/pvsnp.pdf) were rechecked, and the reconfirmation date was added to the existing model note.

**Gap, target, and test.** The selected checkpoint action tested whether unary padding supplies one fixed polynomial bound for explicit computation certificates without losing disagreement with a candidate decider on the same full input. Its plausible downstream use was NP membership for a diagonal language; complete coverage of deciders and sound, complete certificates remained separate requirements. The test compared full-input simulation with shorter-core simulation. It required actual long runs as a failure witness, not a comparison of nominal clock upper bounds. This is a different proposed repair from arbitrary succinct-trace validation, so the previous negative result does not subsume the test.

**Result and decision.** [L003](../lemmas/L003-unary-padding-diagonalization.md) proves the full-input construction is outside P while its explicit transcripts have no uniform polynomial witness bound; it also gives a uniform polynomial-time decider for the shorter-core construction. The latter is applied to its own decider to pinpoint the lost contradiction. These results complete the discriminating test and stop padding as this certificate repair. The [attempt note](../ATTEMPTS/003-unary-padding-explicit-traces.md) preserves that specific failure; [the saved sketch](../drafts/2026-09-25-padding-audit.md) is archived as completed. No novelty or P-versus-NP resolution is claimed.

**Required versus achieved bound.** For each proposed C(N+1)^d witness bound, the proof supplies a fixed machine and clock whose yes-instances have actual transcript length at least (N+1)^(d+1) along arbitrarily long padding, exceeding the bound by an unbounded factor. Conversely the shorter-core version permits only a clock at most N and admits one fixed polynomial simulation bound. The former is a certificate-format obstruction, not an unconditional lower bound against all verifiers; the latter is an upper bound for a different language.

**Effect on the gap and reason for the subsequent direction.** No language is proved to lie in NP outside P. Padding cannot provide the missing certificate compression here. A materially different intermediate mechanism is an exact affine encoding of configurations, for which repeated squaring could evaluate long transition blocks efficiently when the dimension is polynomial. Before attempting general machines, the concrete stress test is a cyclic binary counter with affine decoding; a dimension lower bound would reject that proposed representation. General-machine encoding and useful certificates would remain unresolved even if the small test succeeded. This records the reason for the single action in PROGRESS.md, without starting that test. The padding assessment is complete with informative negative evidence, so exploration turns without an advance or informative negative remain 0.

**Review and verification.** The proof was checked for unique parsing, inclusion of unary exponents and padding in N, arbitrarily large padding with one fixed core, clock constants, genuine running-time lower bounds, deterministic uniqueness of explicit transcripts, and a uniform shorter-core simulator. Neither L001 nor L002 is a mathematical input, so L003 has an empty DAG row. Mathlib coverage is not checked. No numerical experiment is needed for these quantified symbolic arguments. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem p-vs-np` passed with three nodes and no edges; the environment flag prevents bytecode writes to shared infrastructure. `git diff --check -- .` also passed. These checks establish documentation consistency, not mathematical correctness.
