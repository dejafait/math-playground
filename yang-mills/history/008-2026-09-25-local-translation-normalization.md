# 2026-09-25 — Local translation normalization obstruction

Preserved the existing changes after reading the required shared/local files, complete overview and DAG, relevant proof, and earlier failures. Reopened the official Jaffe–Witten statement; the recorded target is unchanged. The [saved preflight](../drafts/2026-09-25-local-translation-normalization.md) identifies the matching gap and the test of whether a local Ward condition isolates the triplet coefficient.

[L007](../lemmas/L007-local-translation-normalization-obstruction.md) proves that a nonzero compact displacement necessarily has off-diagonal strain. Its divergence-free localization removes the scalar channel but leaves shear in the transition region. It retains the affine boundary flux and derives the exact finite-product Haar identity with its measure-divergence term. The resulting conditional triplet/shear equation still needs a probe cancellation or independent matching data and residual control. The [source qualification](../foundations/05-lattice-stress-tensor-matching.md#local-translation-identities) records the separate generator normalization and symmetry-restoration assumption.

Decision: NEGATIVE. The [failed shortcut](../ATTEMPTS/004-triplet-only-compact-translation.md) is an operator-level geometric projection, not the general Ward-identity method or the Yang–Mills target. Joint triplet/shear response is the justified subsequent direction because a nonzero response determinant could supply the missing algebraic identifiability. No response determinant, finite matching limit, or interacting reflection error <= c_box/2 was established. Exploration turns used: zero inconclusive turns. STATUS remains IN_PROGRESS; no complete candidate exists.

Updated the compact checkpoint and overview. The sole new DAG input is the site-centered triplet and its normalization from L006; the geometric and Haar calculations are proved directly. Mathlib coverage remains not checked. Mathematical review checked the mixed-derivative identity, compact-support argument, classical boundary flux, trace cancellation for the continuous and discrete curls, and Haar integration sign. No numerical check is needed for these exact identities.

Validation: `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem yang-mills` passed with seven nodes, seven edges, an acyclic graph, complete file coverage, valid links, and compact overviews. `git diff --check -- .` passed. Only the active notebook was edited; prior work and inactive branches were preserved.
