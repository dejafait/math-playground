# 002 — 2026-09-25 — Source and endpoint audit

Read the required shared/local files and empty DAG; inspected and preserved
the earlier local source audit and unrelated notebook changes. No existing
lemma or failed mathematical route was duplicated. This was one source audit
with a discriminating endpoint test, not a second bounds-research step.

The gap was the unread current definition and its endpoint convention. The
intermediate target was a cited, versioned event whose quantifiers could be
checked before choosing a counting argument. Direct PDF/archive recovery
remained unsuccessful. The new mechanism was to trace the official companion
project's pinned upstream library. The
[model freeze](../foundations/02-pinned-affine-line-model.md) records the
readable event, sampler, domains, exact rates, and grid convention, while
preserving the missing July-paper correspondence.

The discriminating test asked whether a grid certificate can supply the
literal largest safe real radius. [L001](../lemmas/L001-affine-support-radius-cells.md)
proves the relevant safe set is right-open. The
[smooth witness](../lemmas/C001a-smooth-endpoint-witness.md) realizes a
nonempty proper safe set at the actual 2^-128 error target. This changes the
source decision: an automatic grid-to-real-maximum transfer is ruled out,
as recorded in [the failed approach](../ATTEMPTS/001-grid-boundary-as-real-maximum.md).
It neither locates the sharp grid threshold nor improves a beyond-Johnson bound.

Outcome: NEGATIVE, an informative mathematical obstruction to that transfer.
The consecutive exploration count is now zero; the earlier exploration is
preserved. The conditional application is an UNVERIFIED CANDIDATE, not a
verified disproof. Its weakest step is identifying the frozen model and
allowed parameters with the current primary challenge. That is why critical
candidate review takes priority over further mathematical branches.

Validation: `python3 scripts/endpoint-audit/check.py` passed the exact field-size,
divisibility, root-count, and pair-sum checks. The only DAG input is the use of
L001's monotonicity and endpoint conclusion in C001a; standard definitions and
finite-field facts are in the foundation.
`python3 ../scripts/docs/check_structure.py --problem reed-solomon-mca` passed:
2 nodes, 1 edge, valid links, and compact overviews. It checks structure, not
the mathematics or the unverified source correspondence. `git diff --check -- .`
also passed.
