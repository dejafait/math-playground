# 2026-09-25 — Primitive-divisor existence leaves multiplicity open

One focused audit tested whether the divisor theorem could exclude the complete repeated-cube coefficient system. [L008](../lemmas/L008-primitive-divisor-multiplicity.md) checks the Lucas hypotheses, applies the precise theorem and small-index classification, and proves a congruence restriction on its primitive divisor. The achieved bound k>=6p-1 is only a lower bound; no signature is excluded.

The new discriminating evidence is an infinite construction at p=31 in which 373 is primitive with any prescribed positive multiplicity, including 31. It retains the norm restrictions, the first-case shape of t, and the strict trace inequality. It does not impose the complete perfect-power equality. Thus the outcome is NEGATIVE for the direct existence-to-forbidden-multiplicity inference, with exploration turns used 0/3. The [attempt record](../ATTEMPTS/004-primitive-divisor-multiplicity-inference.md) limits this stop; a theorem about some other divisor under the complete equation remains possible. No complete Beal candidate appeared.

The [exact controls](../scripts/primitive-divisors/results.json) at multiplicities 1,2,31 pass the recurrence, polynomial, norm, coprimality, primitive-rank, and positivity checks. All three fail the full power condition modulo 311. Reproduce with `python3 scripts/primitive-divisors/check_multiplicity.py`. The full construction and source qualifications are in the [draft assessment](../drafts/2026-09-25-primitive-divisor-audit.md). Mathlib coverage remains not checked.

The reason for the next direction is that this divisor theorem gives neither the missing valuation nor an upper bound. The smaller fixed signature (3,3,5) is still unexcluded by the assembled citations and supports an independent bounded classification audit. The exact next action is only in PROGRESS.md. Existing branches and unfinished work are preserved; the added DAG edge uses L007's parameter facts and coefficient formula, not the failed local routes or an assumed perfect-power solution.
