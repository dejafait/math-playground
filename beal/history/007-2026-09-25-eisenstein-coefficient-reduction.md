# 2026-09-25 — Global extraction retains the coefficient condition

One focused step completed the Eisenstein-parameterization action. The gap was emptiness of the constrained repeated-cube system II. The intermediate target was a global power extraction with an explicit coefficient constraint and exact common-factor valuations, useful for testing a descent or divisor argument. The earlier isolated-factor and exceptional-prime failures do not already decide that target.

[L007](../lemmas/L007-eisenstein-coefficient-reduction.md) proves the extraction, the recurrence equation, its exact 3-adic valuation, the two allocations at p, and a converse retaining positivity. This is a relevant intermediate input, so the outcome is ADVANCE and exploration turns used remain 0/3. It proves no new signature exclusion and supplies no complete candidate. The full assessment is in [the draft](../drafts/2026-09-25-eisenstein-coefficient-reduction.md).

The required threshold remains zero solutions of the full simultaneous system. The achieved coefficient shapes are a signed pth power or p times one, with no upper height bound. The reason for the next direction is that a divisor argument must control multiplicity to conflict with these shapes; merely finding a prime divisor would not suffice. No primitive-divisor theorem has yet been applied. Other residual Beal signatures remain untouched.

The proof review checked norm-Euclidean division, absorption of units, coprime conjugates, half-integer coordinates, signs, the exceptional common factor p, and the converse's parity, primitivity, and strict positivity. L003 is used for its integer reconstruction; L005 motivates the explicitly stated hypotheses, while L006 records a contrast rather than an input. The DAG adds only the genuine mathematical input, and all previous branches and unfinished work are preserved. Mathlib coverage is not checked.

The exact-arithmetic check returned PASS on 4708 coprime signed parameter cases, including 816 with p dividing t and 624 with large 3-adic valuation. Independent ring multiplication, recurrence, and binomial calculations agree. The prior isolated-factor witness still fails the coefficient condition. Reproduce with `python3 scripts/eisenstein-coefficient/check_coefficients.py`; the [saved output](../scripts/eisenstein-coefficient/results.json) records the scope. These bounded controls do not establish emptiness.
