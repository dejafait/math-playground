# Logarithmic coordinate checkpoint — 2026-09-11

Candidate, unproved at checkpoint: for x(t)=sqrt(t) log(t+1)^α, α>1/2, the finite crossing argument gives block average at most 32H log(N+1)^(-2α)(1+log(4N)). Indeed x'(t)≥log(N+1)^α/(4sqrt(N)) on [N,4N]. For j>4N and n<2N, x_n/x_j≤1/sqrt(2). The reciprocal-square tail above M≥2 is at most (log M)^(1−2α)/(2α−1). These estimates should control both the entire far tail and reflection.

Resume by checking the derivative, decreasing-integrand comparison, exact higher-zero enumeration, and fixed-α limit, then store a proved lemma and graph edges. Existing work through Lemma 79 is complete and preserved.

Completed: Lemma 80 proves the candidate with explicit constants. The derivative, full tails, crossing count, and limiting exponent were checked; no unfinished claim is promoted.
