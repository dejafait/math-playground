# General power-coordinate checkpoint — 2026-09-11

Candidate, unproved at this checkpoint: Lemma 78 extends to every fixed p>1/2 with x_n=n^p. On N<=n<j<=4N the derivative has lower bound c_p N^(p-1), where c_p=p min(1,4^(p-1)); this handles increasing as well as decreasing derivatives. The finite increment-crossing argument gives average <=2 c_p^(-2) H N^(1-2p)(1+log(4N)). The far tail uses q_p=1-2^(-p) and the integral of t^(-2p); reflection uses the same integral. All terms vanish for fixed p>1/2.

Resume by verifying the derivative bound for both ranges of p, the tail exponent and constants, exact zero enumeration, and dependency edges before promoting. Existing work through Lemma 78 is complete and preserved.

Completed: Lemma 79 proves the candidate with explicit constants for every fixed p>1/2. Both derivative regimes, the finite crossing count, and all infinite tails have been checked. No unfinished claim from this checkpoint is used as a proved input.
