# Exhaustion checkpoint — 2026-09-10

Completed as Lemma 62 after analytic review. This file retains the initial checkpoint reasoning; the lemma contains the final statement and qualifications.

Scope: for a fixed compact real parameter interval, assume bounded open domains covering the plane, each with boundary zero-free throughout the interval, and reality at the initial parameter. Apply Lemma 61 separately and cover each zero. No common positive boundary margin across domains is needed, nor a limit of zero counts.

Quantitative sufficient condition to check: on each boundary, at a finite parameter mesh, a positive minimum exceeds h times a uniform bound for |F_lambda|, where every parameter is within h of a mesh point. The integral in Lemma 58 bounds this derivative for each finite spatial radius. The unproved part for theta is a sequence of boundaries and mesh lower bounds satisfying this inequality, plus initial global reality. Compact upper bounds alone do not yield lower bounds, and isolated zeros at each slice do not give a common boundary over all slices.

Original planned completion: write and review the exhaustion proof and mesh criterion, explicitly distinguish uniformity in parameter from uniformity across domains; then update DAG/history/progress and validate.
