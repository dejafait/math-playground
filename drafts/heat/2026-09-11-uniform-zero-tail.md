# Uniform zero-tail checkpoint — 2026-09-11

Draft reasoning, not yet promoted. On |λ|≤L the n=1 positive kernel summand on 0≤u≤1 gives F(λ,0)≥m_L=4π(2π−3) exp(−L−πe²). Set B_L=max(0,log(C/(2m_L))+2L²/π). Jensen at radius 2t gives N_λ(t) log 2≤B_L+(t+4)log(t+4)≤B_L+5t log(5t), t≥1. Boundary zeros can be handled by radii decreasing to 2t from above. Tonelli then gives the full radial tail ≤[B_L/R²+10(log(5R)+1)/R]/log 2.

Resume: audit the lower bound, Jensen boundary limit, multiplicities and strict cutoff, then store L130 and update DAG/history/progress. The horizontal tail is bounded by the radial tail without a strip assumption. No moving-supremum conclusion is intended.

Audit completed and result stored in L130. The explicit normalization, boundary-radius limit, strict tail cutoff, full multiplicities and integral constants were checked. This draft is retained as the completed checkpoint.
