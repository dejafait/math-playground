# Finite prime band checkpoint — 2026-09-14

L205 is complete. This step studies primes z<p<=Y with both cutoffs
fixed before N tends to infinity. Proposed result: their joint-divisor
union on the actual box has upper limiting relative size at most
2 sum_(z<p<=Y) p^(-2). The selected set is a subset, so this also
bounds its deletion in this finite band.

Resume: prove rounded-root equidistribution modulo fixed p on b
progressions, prove uniform distribution of a and b divisibility in
the pair box, and distinguish the finite-band limit from the unbounded
prime tail. These claims are draft and unproved at this checkpoint.

Completed: L206 proves the fixed-band bound, using the exact square
root rather than a Taylor surrogate. The growing-prime inference is
archived as an obstruction; the residual tail is explicitly unproved.
