# Iterated-log coordinate checkpoint — 2026-09-11

Candidate, unproved at checkpoint: put L(t)=sqrt(log(t+1))(log(log(t+e)))^β, β>1/2. It is positive and increasing for t>0. For N≤n<j≤4N, x_j−x_n≥L(N)(sqrt(j)−sqrt(n))≥L(N)(j−n)/(4sqrt(N)). The finite increment crossing argument should give 32H(1+log(4N))/L(N)^2. For M≥3, reciprocal-square tails are bounded by the integral of 1/(t log(t)(log(log(t)))^{2β}), equal to (log(log(M)))^{1−2β}/(2β−1). Far separation uses q=1−1/sqrt(2); reflection uses the same tail without q.

Resume by verifying the increasing multiplier, integral comparison, crossing count, and limiting bounds, then write the lemma and graph edges. Earlier work through Lemma 80 is complete and preserved.

Completed: Lemma 81 proves the candidate. Both infinite remainders, finite crossing bound, and limiting estimates have been checked; no unfinished claim was promoted.
