# 2026-09-25 — Adding the diagonal retains the transverse obstruction

STEP_ID: 2026-09-25-hodge-008-cubic-diagonal-union-test. Outcome: NEGATIVE.

Preserved the existing changes and inactive branches. Performed one first-order lifting test for C union Delta_S, with the target and unfinished reasoning saved in the [draft](../drafts/2026-09-25-cubic-diagonal-union-test.md). The intended downstream use was to extend U+id and subtract the diagonal, realizing the cubic RM generator beyond the known family.

[L009](../lemmas/L009-diagonal-union-retains-cubic-obstruction.md) identifies two elliptic double curves and proves that their local smoothing parameters cannot occur in a global lift. A meromorphic vector field on S would have a degree-four base component vanishing at 21 nodal values. This forces the relative diagonal to persist. The residual lift of C extends across the three isolated points by Hartogs for the normal sheaf computed in L008. Conversely the known family gives flat unions, including at those three-branch points. Hence the kernel remains exactly three-dimensional, versus four required RM tangent dimensions.

This new obstruction stops [adding the diagonal](../ATTEMPTS/006-adding-diagonal-to-cubic-support.md); it does not stop all changed representatives. A fibre-product component has zero transcendental action and no dominant identity projection, so it avoids the specific extraction argument and motivates the next test. The achieved Hodge span remains 21 only on the known family. The universal target remains unresolved, STATUS stays IN_PROGRESS, no complete candidate exists, and exploration turns used are 0 after this informative negative result.

Critical review checked that both ambient factors use the same S_A, the two intersection fibres are smooth, the 21 nodal values are distinct, and local smoothing is distinguished from global lifting. At the isolated points the proof extends a normal section rather than assuming normalization or colon flatness. Flatness of the union along the known family is checked with relative finite-group linearization. Mathlib coverage is not checked; the directly cited Stacks reflexive-module theorem supports only the Hartogs input.

Validation: `PYTHONDONTWRITEBYTECODE=1 python3 scripts/cubic-deformation/check_diagonal_union.py` passed the exact conic, finite intersection, slope, critical-fibre, and tangent-eigenvalue checks. `PYTHONDONTWRITEBYTECODE=1 python3 ../scripts/docs/check_structure.py --problem hodge` passed with 10 nodes and 10 edges, and `git diff --check -- .` passed. Bytecode writing was disabled to keep shared infrastructure read-only. The added DAG inputs are the cycle action, the general ideal-parametrization, and the earlier normal-sheaf/kernel result; graph validation does not verify those mathematical dependencies or the global Hodge target.
