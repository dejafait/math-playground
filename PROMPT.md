# One focused notebook step

Read GOAL.md for the workflow, storage, and validation rules. Read PROGRESS.md for the checkpoint. Inspect existing changes and unfinished work before starting; preserve them. Use PROOF.md for the argument and DAG.md to locate needed lemmas. Historical notes are context, not active instructions.

Before choosing work, apply GOAL.md’s relevance and stopping rules: inspect the global DAG and argument, identify the main bottleneck, and state the intended downstream use and required threshold. Check for redundancy and previously failed routes. If the previous two turns on a route did not improve its bottleneck, audit or park the route instead of adding another technical extension. A strategic audit may end without creating any lemma.

Choose exactly one mode at the START of this invocation by inspecting every lemma/corollary Markdown file's Lean proof status and associated source. The backlog includes missing, unfinished, failed, partial, conditional, stale, or statement-mismatched Lean proofs. A status label, an informal proof, a citation, or a passing proof of a weaker statement does not clear it.

- LEAN CATCH-UP: If any backlog remains, work only on formalizing one or more EXISTING lemmas, normally the first unfinished one in mathematical order, respecting prerequisites. Resume interrupted proof work first when appropriate. Do not invent new mathematical lemmas, strengthen or weaken statements, change the research direction, or pursue the deferred research action. Internal Lean helper theorems are allowed. Keep the existing mathematical hypotheses, conclusion, and informal proof unchanged; update only their formalization commentary, status, command, and embedded Lean code. If a statement seems incorrect or ambiguous, record the precise issue without silently repairing it or claiming validation. Work on another existing lemma when possible; an unresolved issue still blocks research mode.
- RESEARCH: Only when EVERY existing lemma/corollary has a complete, statement-faithful, locally Lake-validated Lean proof, perform one coherent mathematical research step, using the deferred research checkpoint. Write new lemmas or improve the informal argument and update the DAG as needed. Do not write or attempt Lean proofs in this turn. Give every new or mathematically changed lemma an explicit unvalidated Lean status; it becomes backlog for the NEXT invocation. If no valid new result is obtained, preserve the obstruction or unfinished reasoning honestly.

Never switch modes mid-turn, even if the last missing proof is completed. Record the mode and result in a brief dated history entry. Keep PROGRESS.md the sole current status/checkpoint, with exactly one concrete Next action and the deferred research checkpoint preserved during catch-up. Do not create a separate live progress file. Save unfinished work and a precise resumption checkpoint before lengthy calculations or quota exhaustion.

For Lean work, use the local Lake executable and the existing scripts/lean project. If Lake is not accessible, record the blocker and end the step without doing research instead. Validate changed targets with `lake build Rh.LNNN` from scripts/lean (use the corresponding corollary target when needed), audit theorem axioms, and embed exactly the checked source in the lemma Markdown. No sorry, admitted proofs, custom axioms, circular premises, or unproved substitutes for the stated conclusion. Explicitly distinguish partial/conditional work from full validation. Fetch the pinned dependency cache only if needed; do not upgrade toolchains or dependencies to hide failures.

Use the exact common sections and order from L001/L002: Hypotheses, Conclusion, Proof, Mathlib, Lean proof status, Lean proof command, Lean proof code. Explain Mathlib coverage honestly and provide relevant documentation links. Unvalidated entries must not claim a successful command or proof. The synchronization script can check formatting and copy source; it does not validate mathematics or Lean.

Run `python3 scripts/docs/check_structure.py` and the checks appropriate to the chosen mode. Repair issues introduced by the step. Never set STATUS: PROVED merely because the Lean backlog is empty; all RH success criteria in GOAL.md must hold.

Return a short account of mode, result, validation, and next action after this one step. The external loop rereads this prompt for the next invocation and owns repetition, quota waiting, and retries. Do not start an inner agent loop, ask the user to continue, change subscriptions, buy credits, use API keys, or modify the launchers, retry state, or this prompt during ordinary loop turns. Work only in this repository. Do not commit, reset, or discard existing changes.
