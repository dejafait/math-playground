# 2026-09-20 — part 09

Mode: LEAN CATCH-UP, selected after inspecting all 232 lemma/corollary status sections and their associated sources (18 present). Missing sources and the partial L015 and conditional L028 leave backlog; no research was attempted.

Resumed [L015](../lemmas/L015-a-nonnegative-indicator-kernel-can-have-zeros-inside-the-strip.md). Identified its indicator-kernel integral with the two finite interval integrals, and transferred the checked zero value and exponential factorization to H. The hypotheses, conclusion, informal proof, and DAG are unchanged. Existing work was preserved.

Validation: `lake build Rh.L015` from `scripts/lean`; all five theorem axiom audits use only `propext`, `Classical.choice`, and `Quot.sound`. Exact checked source embedded in the lemma; documentation structure checked. The full lemma remains partial. Entirety is the next formalization obligation; the deferred research checkpoint is preserved in PROGRESS.md.
