# Research history — 2026-09-11, part 04

## Selected irregular suffix blocks

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and confirmed the previous step was complete; preserved all earlier work. Saved a draft and resume checkpoint before the detailed audit.
- Proved [Lemma 90](../lemmas/L090-upward-averaging-on-selected-irregular-blocks.md), sharpening finite crossing counts to obtain a selected-block averaging criterion. An explicit coordinate sequence meets it while having divergent suffix weights and zero lower dyadic density. This completes a scoped part of the requested step, not the unrestricted assertion.
- Mathematical validation: audited finite crossing counts, integral bounds, selection of disjoint blocks, normalization and full tails; checked the example's derivative, strict boundary ordering, exact suffix set, reciprocal-square summability, divergent weights, and empty blocks. No numerical certificate is needed.
- Added only the direct edge from Lemma 84 for product and full-tail estimates. Lemmas 88–89 are comparisons, not inputs. PROOF.md is unchanged because the main argument and RH gap are unchanged.
- Decision: retain this sufficient condition and isolate its complementary regime, where the block ratio stays bounded below, as the next concrete question. No counterexample to the unrestricted assertion has been established.
- Validation: `python3 scripts/docs/check_structure.py` passed (91 nodes, 162 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Retaining the block height budget

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes, the current checkpoint and the relevant proofs. The preceding step was complete; preserved all existing work. Saved draft reasoning before the proof audit.
- Proved [Lemma 91](../lemmas/L091-height-budget-selection-on-irregular-blocks.md). Retaining the actual height increase strengthens the block criterion to cover bounded-ratio subsequences and divergent reciprocal-ratio sums over nonempty dyadic blocks. This completes a precisely scoped part of the requested lower-bounded-ratio regime.
- Mathematical validation: checked the finite increment range and telescoping sum, twofold overlap including exact powers of two, nonnegative infinite summation, the divergent-series contradiction, ordered minimizing subsequences, and the full-tail normalization. No numerical certificate is needed.
- Added direct inputs from Lemmas 90 and 84 for the crossing estimate and product/tails respectively. PROOF.md is unchanged because the overall argument and RH gap are unchanged.
- Decision: the remaining regime has ratios tending to infinity and summable dyadic reciprocal ratios. The finite height budget alone yields no contradiction there; no counterexample or unrestricted theorem is claimed. PROGRESS.md records that single residual question.
- Validation: `python3 scripts/docs/check_structure.py` passed (92 nodes, 164 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Spacing-sensitive suffix blocks

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected the existing changes and completed checkpoint; preserved earlier work. Saved draft reasoning before the detailed proof.
- Proved [Lemma 92](../lemmas/L092-spacing-sensitive-height-budget-selection.md), a scoped improvement that uses minimum suffix-index spacing in each block. It supplies conditional near-sum and full-contribution subsequences; it does not resolve the entire requested residual regime.
- Mathematical validation: audited the increment expansion, integer endpoints, reciprocal-square integral bound, backwards spacing estimate, empty and singleton cases, dyadic budget selection, and full tails. No computational certificate is required.
- Added direct graph inputs from Lemmas 90, 91 and 84 for separation, budget and product/tails. The overall argument has not changed, so PROOF.md remains unchanged.
- Decision: the bound removes a logarithmic loss for separated indices, but realizability within all the residual conditions remains unproved. Test that precise construction problem next before claiming this conditional improvement covers a new example.
- Validation: `python3 scripts/docs/check_structure.py` passed (93 nodes, 167 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Realizing separated suffix blocks

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and confirmed the prior spacing step was complete; preserved all earlier work. Saved a draft and resume checkpoint before the full construction audit.
- Proved [Lemma 93](../lemmas/L093-separated-suffix-blocks-realize-the-spacing-criterion.md). Its explicit coordinates meet every requested residual condition while having bounded spacing-sensitive ratios along selected blocks, completing the current construction question.
- Mathematical validation: checked strict coordinate ordering at both gap boundaries, the derivative, exact suffix membership, reciprocal-square summability, divergent suffix weights, empty blocks, the lower ratio bound for every nonempty integer block, and exact dyadic ratios including the singleton prefix. The proof is analytic and needs no numerical certificate.
- Added direct edges from Lemmas 91 and 92 for the ratio definitions and the latter's subsequence theorem. PROOF.md is unchanged because the overall argument and RH gap are unchanged.
- Decision: the spacing refinement has a realized application. Next test whether the exact crowding quantity can extend it past the sensitivity of minimum spacing to a single close pair. No unrestricted theorem or counterexample is claimed.
- Validation: `python3 scripts/docs/check_structure.py` passed (94 nodes, 169 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Exact crowding and paired suffix clusters

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and confirmed the previous construction was complete. Preserved earlier work and saved draft reasoning before auditing the new construction.
- Proved [Lemma 94](../lemmas/L094-exact-crowding-height-budget-selection.md) and [Lemma 95](../lemmas/L095-paired-suffix-blocks-realize-exact-crowding.md). The exact harmonic crowding gives a height-budget criterion, and paired clusters realize it while both minimum-spacing criteria fail. This completes the requested step.
- Mathematical validation: audited the finite increment cutoff, harmonic tail, positivity and dyadic budget selection; checked interpolation boundaries, exact suffix membership, summability, divergent suffix weights, empty blocks, every-block growth via three size cases, exact dyadic ratios, and the two-grid crowding bound. All proofs are analytic; no numerical certificate is required.
- Added direct graph inputs for the reused increment expansion, height budget, product tails, ratio definitions and interpolation argument. PROOF.md remains unchanged because the overall argument and RH gap are unchanged.
- Decision: exact crowding removes sensitivity to isolated close pairs. Investigate selection within blocks next, since the maximum crowding can still be dominated by a small dense portion. This possibility is unproved and is not a DAG input.
- Validation: `python3 scripts/docs/check_structure.py` passed (96 nodes, 175 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Selecting subsets inside suffix blocks

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and the completed preceding checkpoint, preserving all earlier work. Saved an explicitly unproved draft and resume checkpoint before the detailed proof audit.
- Proved [Lemma 96](../lemmas/L096-selected-subset-crowding-selection.md) and [Lemma 97](../lemmas/L097-endpoint-packets-realize-subset-selection.md). Restricted averaging has a realized application where both full-block exact-crowding criteria fail, completing the requested research step.
- Mathematical validation: audited restricted separation and finite increment sums, height-budget selection and full tails; checked interpolation, exact suffix membership, weight divergence, empty blocks, endpoint-packet containment for every shifted block, harmonic lower bounds and dyadic summability. Supplemental finite checks at d=8,16,32 passed for all 87,303,1119 nonempty shifted blocks respectively and for each subset bound. These checks are sanity checks; the analytic proof covers all construction scales without a numerical certificate.
- Reviewed direct graph inputs: Lemma 96 uses the definitions in 94, restricted expansion from 92, budget from 91 and product/tails from 84; Lemma 97 uses 94's definitions, 95's interpolation and 96's criterion. PROOF.md is unchanged because the main argument and RH gap are unchanged.
- Decision: isolate the best ratio available to any nonempty subset before proposing another refinement. Whether these optimized ratios can fail both criteria is left unproved and recorded as the single next action.
- Validation: `python3 scripts/docs/check_structure.py` passed (98 nodes, 182 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Optimized subset obstruction

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and confirmed the preceding step was complete; preserved all earlier work. Saved a draft and resume checkpoint before the proof audit.
- Proved [Lemma 98](../lemmas/L098-consecutive-packets-obstruct-optimized-subset-criteria.md), settling the current construction question affirmatively. Archived the failed universality claim in [ATTEMPTS](../ATTEMPTS/2026-09-11-optimized-subset-universality.md).
- Mathematical validation: checked rank-gap multiplicities, finite Cauchy bounds, small-subset and large-subset cases, interpolation endpoints, exact suffix membership, reciprocal-square summability, divergent weights, all clipped blocks and exact dyadic support. Supplemental Python Fraction enumeration checked the crowding bound and every rank-gap bound for all 255, 4095 and 65535 nonempty subsets of packets of lengths 8, 12 and 16; these are sanity checks only. The proof covers d≥256 analytically.
- Added direct graph inputs from Lemmas 94 and 96 for the definitions; interpolation and the finite crowding lower bound are proved directly. PROOF.md is unchanged because the overall argument and RH gap are unchanged.
- Decision: failure of optimized upper bounds does not establish failure of actual near-sum selection. Investigate packet endpoints using actual coordinate gaps next; that conclusion remains unproved.
- Validation: `python3 scripts/docs/check_structure.py` passed (99 nodes, 184 edges); `git diff --check` passed. Status remains IN_PROGRESS.

## Common subsequence at consecutive packet endpoints

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and confirmed the preceding step was complete. Preserved earlier work and saved a draft and resume checkpoint before the proof audit.
- Proved [Lemma 99](../lemmas/L099-common-upward-subsequence-at-consecutive-packet-endpoints.md): the requested fixed packet endpoints work for every permitted height sequence. The optimized criteria's failure does not prevent actual upward vanishing in this construction.
- Mathematical validation: checked all future normalized-coordinate cases, the explicit interpacket ratio, positive gap, exact near-term count, full-contribution normalization, and reciprocal-square tails. This analytic proof requires no numerical certificate.
- Reviewed direct graph inputs from Lemma 98 for the construction and its properties and Lemma 84 for the paired product and tail bounds. PROOF.md remains unchanged because the main argument and RH gap are unchanged.
- Decision: investigate whether arbitrary prescribed sets in this same interpolation family admit upward selection; the result is not yet proved. Status remains IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (100 nodes, 186 edges); `git diff --check` passed.

## Arbitrary prescribed interpolation

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity; exact backend variant not exposed).
- Inspected existing changes and the completed Lemma 99 checkpoint; preserved all prior work. Saved draft reasoning and a resume checkpoint before the detailed proof.
- Proved [Lemma 100](../lemmas/L100-upward-averaging-for-arbitrary-prescribed-interpolation.md): every arbitrary unbounded prescribed set in the specified interpolation family admits vanishing full-upward block averages and therefore a vanishing subsequence. This completes the stated existence question with selection among all indices.
- Mathematical validation: checked the derivative lower bound, first/interior/last gap differences and adjacent prescribed indices, telescoping global separation, reciprocal-square summability, exact higher-zero enumeration, finite increment crossing count, constants 64 and 18, integral tails, and increasing dyadic minimum selection. The proof is analytic and requires no numerical certificate.
- Reviewed direct graph inputs: Lemma 74 supplies product properties and reflected tails; Lemma 78 supplies the reused finite crossing argument. The interpolation properties are proved directly, so Lemmas 98 and 99 are motivation rather than mathematical inputs. PROOF.md remains unchanged because the overall argument and RH gap are unchanged.
- Decision: the averaging selects among all indices and can depend on the heights. Whether selection can always remain inside P is a distinct stronger question and is the next action. No unrestricted-coordinate theorem or RH result is claimed.
- Validation: `python3 scripts/docs/check_structure.py` passed (101 nodes, 188 edges); `git diff --check` passed. Status remains IN_PROGRESS.
