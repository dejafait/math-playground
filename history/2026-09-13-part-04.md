# Research history — 2026-09-13, part 04

## Positive quartic stationary product strip

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing tracked and untracked changes and the completed L180 checkpoint; preserved earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-13-positive-quartic-kernel.md) and a resume action before completing the calculation.
- Proved [L181](../lemmas/L181-positive-quartic-stationary-product-strip.md): exact integer stationary criterion, uniform transition bound, and separate absolute stationary/collar/exterior estimates. This completes the scoped separation; signed stationary cancellation remains unproved. No failed route is declared: the weaker stationary bound is an explicit limitation of absolute summation.
- Direct graph input is L180 for the actual block, profiles and phase kernel. Analytic verification checked both derivatives, the endpoint criterion, integration by parts, transition length, fixed-three-index counts, shell boundaries and conjugate coefficient. No external theorem or computational claim is required.
- Decision: retain endpoint transitions in a stationary-phase expression before attempting the signed product sum. PROOF.md stays unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (182 nodes, 342 edges); `git diff --check` passed. No numerical evidence is used as proof.

## Endpoint-uniform quartic stationary phase

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected and preserved existing changes; L181 was complete, with no interrupted extension. Saved a [draft checkpoint](../drafts/heat/2026-09-13-quartic-endpoint-stationary-phase.md) and resume action before proving the formula.
- Proved [L182](../lemmas/L182-endpoint-uniform-quartic-stationary-phase.md) by an exact quadratic coordinate. The finite Fresnel factor keeps both endpoint transitions; the kernel error is summable with an O(1) total bound. No signed cancellation claim is promoted.
- Direct graph inputs: L179 for the exact phase constant and L181 for the stationary region, weights and counting bound. Checked the smooth inverse, Jacobian, boundary terms and normalization analytically.
- Decision: examine perfect-square products with the real projection retained, before claiming cancellation of the product sum. No failed approach is declared. PROOF.md is unchanged because the overall argument is unchanged.
- Validation: `python3 scripts/docs/check_structure.py` passed (183 nodes, 344 edges); `git diff --check` passed. Mathematical verification is the analytic calculation in L182; no numerical evidence is used.
