# Research history — 2026-09-14, part 07

## Coprime divisor-phase reduction

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected existing changes and the completed L227 checkpoint; preserved prior work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-divisor-phase-discrepancy.md) before the calculation.
- Completed a scoped part of the aggregate discrepancy task in [L228](../lemmas/L228-coprime-divisor-phase-discrepancy.md): the occupancy discrepancy is an exact divisor-phase sum up to negligible O(N³) boundary and benchmark errors. Coprimality and strict endpoint atoms are retained. Cancellation remains unproved.
- Reviewed direct mathematical inputs L221 (interior box and scales), L225 (cells and exact occupancy), and L227 (count and benchmark). Checked the crossing-cell ranges, derivative identity, strict integer endpoints, finite inversion and real area calculation analytically.
- Decision: investigate the large-divisor contribution before attempting a phase estimate with a growing cutoff. No cancellation theorem was asserted or disproved, so no failed-attempt archive was added. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/heat/check_coprime_cell_inversion.py` passed 12,800 exact inversions with 1,705 upper endpoint atoms. `python3 scripts/docs/check_structure.py` passed (229 nodes, 475 edges). Finite checks verify identities only.
- Additional validation: existing fractional-cell checks passed 4,898 cases and 924 weighted reindexings; `git diff --check` passed. Structural validation was rerun after updating the checkpoint and history.

## Large-divisor tail permits a growing cutoff

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected existing changes and the completed L228 checkpoint; preserved all earlier work. Saved the calculation before completion in the [tail draft](../drafts/heat/2026-09-14-large-divisor-phase-tail.md).
- Proved [L229](../lemmas/L229-large-divisor-tail-for-the-paired-cell-discrepancy.md): the large-divisor discrepancy is negligible for every fixed positive power cutoff below N. The paired endpoint identity turns the tail into counts of common multiples; an elementary ordered-product bound controls these counts without phase distribution assumptions.
- Reviewed direct mathematical inputs L228 (exact cells, endpoint identity and interval bounds) and L227 (benchmark lower bound). The new node uses only these established inputs. No failed approach or change to the overall argument occurred; PROOF.md remains unchanged.
- Decision: fix D=N^(1/4) and investigate a controlled finite Fourier approximation of the retained paired phases. Their cancellation and all subsequent comparisons remain unproved; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/heat/check_large_divisor_cell_tail.py` passed 35,236 strict-cell counts and product bounds, including 5 upper endpoint atoms. `python3 scripts/docs/check_structure.py` passed with 230 nodes and 477 edges. Analytic checks prove the asymptotic bound; finite checks only verify identities and enlargements.

## Finite Fourier approximation with controlled endpoint atoms

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected the existing changes and completed L229 checkpoint, preserving all earlier work. Saved the [Fourier draft](../drafts/heat/2026-09-14-finite-fourier-endpoints.md) before the proof calculation.
- Proved [L230](../lemmas/L230-finite-fourier-reduction-with-endpoint-control.md): the retained paired discrepancy admits a finite Fourier approximation with aggregate error o(B_0). Exact strict endpoint half-atoms and near-integer phases are negligible by two elementary product counts, uniformly without a separation assumption on R.
- Reviewed direct inputs L228 (cells and discrepancy), L229 (divisor estimates and tail), and L227 (benchmark lower bound). Checked the centered sawtooth convention, ordinary Fourier tail, separate lower/upper product counts, divisor multiplicity and aggregate powers analytically.
- Decision: test a summed second-derivative estimate for the explicit finite sum. The large Fourier cutoff may make this insufficient; no cancellation estimate is claimed. No failed approach was asserted, so no ATTEMPTS entry was needed. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/heat/check_fourier_endpoint_control.py` passed 2,562 strict endpoint identities (174 atom cases), 28,812 product identities and 508 Fourier samples. `python3 scripts/docs/check_structure.py` passed with 231 nodes and 480 edges. Finite tests check identities and samples only, not the asymptotic theorem.

## Second-derivative Fourier budget is insufficient

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected existing tracked and untracked work and the completed L230 checkpoint; preserved it all. Saved the [calculation checkpoint](../drafts/heat/2026-09-14-second-derivative-fourier-budget.md) before writing the proof.
- Proved [L231](../lemmas/L231-second-derivative-budget-for-the-paired-fourier-sum.md): the explicit paired second-derivative estimate, its aggregate frequency/divisor bound, and the stronger but still insufficient uniform sawtooth bound. Recorded [why the method fails](../ATTEMPTS/2026-09-14-second-derivative-fourier-budget.md). No lower bound for the actual discrepancy is claimed.
- Reviewed direct inputs L227 (benchmark), L228 (interval geometry and length derivative), and L230 (finite sum and uniform Fourier boundedness), and the established named second-derivative input in foundations. Analytically checked both exact second derivatives, the amplitude variation, both frequency sums, the divisor count and relative scales. This purely analytic step requires no numerical inference.
- Decision: test the third derivative only in the explicit low-frequency range k<=N^(1/4), leaving higher frequencies unresolved. PROOF.md is unchanged because the overall argument has not changed; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (232 nodes, 483 edges); `git diff --check` passed. Structural checks establish storage and graph invariants, not mathematical correctness.
