# 2026-09-20 — part 23

Mode: LEAN CATCH-UP. Inspected all 232 lemma/corollary status sections and associated source presence and synchronization; the backlog remains nonempty. Resumed L017 without changing its hypotheses, conclusion, or informal proof, preserving prior work.

[L017](../lemmas/L017-split-mellin-integral-with-entire-remainder.md) now has checked continuity and absolute convergence of the literal split integrand for every complex parameter. The Gaussian integrability theorem is in the root namespace; explicit unfolding of `IntegrableOn` resolves the comparison's elaboration boundary.

Validation: `lake build Rh.L017` succeeded with Lean 4.33.1 and the existing pinned mathlib. All seven theorem axiom audits contain only `propext`, `Classical.choice`, and `Quot.sound`. The exact checked source is embedded. Documentation structure check passed.

L017 remains partial: entireness and the gamma–zeta identity are open. The next direction is dominated parameter differentiation using the additional logarithmic factor. PROGRESS.md retains the deferred research checkpoint; no research was performed.
