# Research history — 2026-09-13, part 03

## Last-block endpoint profile reduction

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing tracked and untracked changes and the completed finite probe; preserved earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-13-last-block-first-moment-reduction.md) before the analytic audit.
- Proved [L173](../lemmas/L173-last-block-endpoint-profile-reduction.md), a scoped part of the last-block first-moment task: endpoint coefficient replacement, exact Abel reduction and a sufficient unproved cancellation condition. Mean-square estimates do not give the required decay. No asymptotic conclusion is inferred from the finite probe.
- Checked constants and powers in variance normalization, derivative bounds, both integration endpoints, the harmonic frequency sum, mesh endpoint conventions and the decreasing weight's sign. Direct graph inputs are L169 for the frozen block definitions and L170 for amplitudes and V.
- Decision: focus the remaining estimate on unweighted partial Dirichlet sums on the actual moving block. PROOF.md remains unchanged because the overall argument has not changed; STATUS stays IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (174 nodes, 329 edges); `git diff --check` passed. Mathematical verification is the analytic audit recorded in L173; no computational assertion requires a numerical certificate.

## Classical derivative-test audit

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected tracked changes, untracked research artifacts and the completed L173 checkpoint; preserved all existing work. Saved a [draft checkpoint](../drafts/heat/2026-09-13-derivative-test-first-moment.md) before completing the estimate.
- Proved [L174](../lemmas/L174-derivative-test-limit-for-last-block-first-moments.md), testing the classical derivative estimates uniformly over truncated sums. Recorded the limited failure in [ATTEMPTS](../ATTEMPTS/2026-09-13-pointwise-derivative-first-moment.md). The desired averaged decay remains unproved, not disproved.
- Added the named derivative tests and checked primary-source statements to foundations. L174 is self-contained over those standard inputs: it repeats the short finite square calculation and only cites L173 to explain the target, so no lemma dependency edge is added.
- Decision: test a stationary-phase transform retaining dual phases and controlling endpoint errors. PROOF.md is unchanged because the overall argument is unchanged. STATUS stays IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (175 nodes, 329 edges); `git diff --check` passed. Analytic verification checked derivative scales, truncation endpoints, the averaged square error and both first-moment inequalities; no computational assertion requires numerical testing.

## Full-endpoint stationary transform

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and completed L173–L174 work; preserved all earlier artifacts. Saved a [draft checkpoint](../drafts/heat/2026-09-13-last-block-stationary-transform.md) before checking the transform theorem.
- Proved [L175](../lemmas/L175-last-block-stationary-transform.md) with uniform logarithmic error, including near-integer endpoint slopes. Checked the C^4 B-process in Vandehey Theorem 1.1; the older C^3 estimate was insufficient. Added the standard input to foundations.
- Recorded the limitation of transform plus second moments in [ATTEMPTS](../ATTEMPTS/2026-09-13-transform-alone-first-moment.md). No decay is claimed. L174 supplies the parameters and moment estimates, and is the sole direct graph input.
- Decision: quantify the moving dual boundary strips before comparing the transformed sum to a fixed interval. PROOF.md stays unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (176 nodes, 330 edges); `git diff --check` passed. Analytic verification checked derivative hypotheses, conjugation and phase normalization, bounded endpoint changes, uniformity in t and transfer of moments. No numerical certificate is needed.

## Fixed dual interval with controlled boundary strips

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing tracked and untracked work and the completed L175 checkpoint; preserved all earlier changes. Saved a [draft checkpoint](../drafts/heat/2026-09-13-fixed-dual-boundaries.md) before completing the boundary calculation.
- Proved [L176](../lemmas/L176-fixed-dual-interval-with-small-boundary-error.md): replacing the moving dual interval by [N,2N] has O(N^(1/4)) first absolute moment of the difference, with the original amplitude retained. No complete-sum decay is inferred.
- Analytic verification checked both strip supports, strict/closed endpoints, pairwise indicator intersections, amplitude variation in integration by parts and the normalized harmonic sum. L175 is the direct graph input for definitions and the final transform comparison. Corrected the stale lemma count in the DAG introduction.
- Decision: next examine uniformity over primal truncations, the remaining scope needed for the earlier partial-sum criterion. PROOF.md is unchanged because the overall argument has not changed; STATUS remains IN_PROGRESS. No failed attempt or computational certificate arose.
- Validation: `python3 scripts/docs/check_structure.py` passed (177 nodes, 331 edges); `git diff --check` passed.

## Uniform truncated dual interval

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing tracked and untracked work and the completed L176 checkpoint; preserved all prior changes. Saved a [draft checkpoint](../drafts/heat/2026-09-13-uniform-truncated-dual-interval.md) before the endpoint audit.
- Proved [L177](../lemmas/L177-uniform-truncated-fixed-dual-interval.md): the stationary transform and fixed dual boundary replacement have uniform little-o first-moment error for every fixed primal truncation. The signed indicator identity handles overlapping strips without assuming a minimum truncation length.
- Analytic validation checked the standard input's M≥interval-length hypothesis, y=N, endpoint conventions, pairwise threshold supports, uniform coefficient variation and harmonic summation. The supremum remains outside the expectation. Direct graph inputs are L174 for truncated-sum definitions, L175 for the transform argument and L176 for the strip estimate.
- Decision: insert this uniform representation into the existing Abel identity and compute the weighted dual profile before claiming any cancellation benefit. PROOF.md is unchanged because the overall argument is unchanged; STATUS stays IN_PROGRESS. No failed attempt or numerical certificate arose.
- Validation: `python3 scripts/docs/check_structure.py` passed (178 nodes, 334 edges); `git diff --check` passed. No numerical experiment is used as proof.

## Weighted dual profile after Abel substitution

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing tracked and untracked work and the completed L177 checkpoint; preserved earlier changes. Saved a [draft checkpoint](../drafts/heat/2026-09-13-weighted-dual-profile.md) before completing the coefficient and error audit.
- Proved [L178](../lemmas/L178-weighted-dual-profile-and-involution.md), completing the uniform dual substitution in Abel's identity. The explicit reflected profile has an involutive norm-preserving transformation; no decaying first-moment estimate follows from the identity alone.
- Analytic verification checked uniform fixed-y integration without a maximal estimate, closed endpoints, scalar freezing through an averaged square bound, reflected Gaussian, coefficient ratio, derivative sign and norm-preserving Jacobian. Direct graph inputs are L173 and L177.
- Decision: examine the two profile components under this involution to make its remaining constraints explicit. This completed reduction is not a failed attempt; no ATTEMPTS entry is needed. PROOF.md is unchanged because the overall argument is unchanged; STATUS stays IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (179 nodes, 336 edges); `git diff --check` passed. The calculation is analytic and requires no numerical certificate.

## Profile eigenspaces and first-moment obstruction

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and the completed L178 checkpoint; preserved all earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-13-profile-eigenspaces.md) and a resume action before completing the calculation.
- Proved [L179](../lemmas/L179-profile-eigenspaces-and-first-moment-obstruction.md), including an L² transform error and the real/imaginary phase constraints. Recorded the failed inference from symmetry and second moments in [ATTEMPTS](../ATTEMPTS/2026-09-13-profile-symmetry-first-moment.md). The abstract models are not asserted to be arithmetic sums.
- Analytic verification checked Minkowski with L177's square estimate, scalar freezing, mixed frequency sums, orthogonality, strict profile correlation, rotation signs, and both exact probability models. Direct graph inputs are L173 for definitions and the square calculation, L177 for the uniform square error, and L178 for the Abel substitution and involution.
- Decision: inspect the mixed fourth moment to seek additional joint-amplitude information. PROOF.md is unchanged because the overall argument is unchanged; STATUS stays IN_PROGRESS.
- Validation: `python3 scripts/docs/check_structure.py` passed (180 nodes, 339 edges); `git diff --check` passed. No numerical certificate is required for this analytic step.

## Exact mixed fourth moment

- Date: 2026-09-13. CLI/model: Codex; GPT-6 (session identity).
- Inspected tracked and untracked work and the completed L179 checkpoint; preserved all existing changes. Saved a [draft checkpoint](../drafts/heat/2026-09-13-mixed-fourth-moment.md) before the moment audit.
- Proved [L180](../lemmas/L180-mixed-fourth-moment-kernel-expansion.md): an exact signed-kernel expansion and the existing-order upper bound. Preserved the [coarse-bound limitation](../ATTEMPTS/2026-09-13-mixed-fourth-moment-coarse-bound.md). No fourth-moment conclusion is inferred from small L² errors.
- Direct graph inputs are L179 for profiles and actual components, and L171 for equal-product counting and the signed endpoint Hilbert argument. Reviewed both inputs mathematically; no new standard input is needed.
- Decision: isolate the all-positive stationary kernel contribution to test whether finer arithmetic information improves the bound. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Validation: `python3 scripts/heat/check_mixed_fourth_algebra.py` passed 1000 deterministic cases; analytic checks cover normalization, signs, kernel derivative and signed coefficients. `python3 scripts/docs/check_structure.py` passed (181 nodes, 341 edges); `git diff --check` passed. The finite check is not asymptotic evidence.
