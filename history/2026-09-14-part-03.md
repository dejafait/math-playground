# Research history — 2026-09-14, part 03

## Near-diagonal integer occupancy obstruction

- Date: 2026-09-14. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and the completed L199 recovery, preserving all earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-near-diagonal-window-exclusion.md) and resume action before completing the calculation.
- Proved [L200](../lemmas/L200-near-diagonal-full-core-exclusion.md): the full-core floor cell has a parity-sensitive near-diagonal exclusion. The excluded ordered quadruples are only O_K(N^(5/2)), so this is a precisely scoped part of the occupancy audit, not the requested N³ lower bound or its negation.
- Decision: retain occupancy outside the excluded band as the next counting problem. No claimed lower-bound proof was abandoned; no failed-attempt file is needed. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Direct graph inputs reviewed: L199 supplies the full-core floor-cell necessity; L197 supplies the aggregate ordered-factor interval count. Analytic verification checked integer midpoint parity, the exact square identities, equality boundaries, and the uniform excluded-population estimate.
- Validation: `python3 scripts/heat/check_near_diagonal_exclusion.py` passed 50,000 exact cases (17,777 occupied floor-cell windows). These checks test algebra only, not asymptotic occupancy.
- Structural validation: `python3 scripts/docs/check_structure.py` passed (201 nodes, 398 edges); `git diff --check` passed.

## Exact full-core phase criterion

- Date: 2026-09-14. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and confirmed L200's checkpoint was complete; preserved all prior work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-full-core-phase-reduction.md) before developing this scoped reduction.
- Proved [L201](../lemmas/L201-full-core-integer-phase-criterion.md), an exact unique-candidate occupancy criterion retaining every safe-m cutoff, and a sufficient interior phase condition using a quartic expansion. The requested N³ lower bound is still unproved.
- Archived the [quadratic truncation failure](../ATTEMPTS/2026-09-14-quadratic-phase-truncation.md), with its precise integer-sector scope. No factor-population counterexample is claimed.
- Decision: next count simultaneous phase and safe-m selection on actual factor tuples. The small phase approximation error alone gives no population estimate. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Reviewed the sole direct graph input L199, which supplies the full-core inequalities. L200 is a consistency comparison, not a mathematical input. Taylor's theorem and elementary rounding are proved or explicitly applied in the new text.
- Validation: `python3 scripts/heat/check_full_core_phase.py` passed 10,000 exact candidate checks (1,528 occupied) and 10,000 rational Taylor bounds. Analytic review checked positivity, perfect-square cases, one-sided remainders, and the fixed-epsilon implication. These checks make no asymptotic count inference.
- `python3 scripts/docs/check_structure.py` passed (202 nodes, 399 edges); `git diff --check` passed.

## Safe factor box before phase selection

- Date: 2026-09-14. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and confirmed L201 was complete; preserved all prior work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-safe-factor-box.md) and resume action before the calculation.
- Proved [L202](../lemmas/L202-safe-factor-box-before-phase-selection.md), a scoped part of the requested simultaneous count: an explicit actual factor box has order N³ tuples, each satisfying all safe-m cutoffs and the opposite-side restriction. The phase-selected lower bound is explicitly unproved.
- Decision: focus the next step on phase selection in this box, whose geometric population is now proved. No failed shortcut or negation of the target count is claimed. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Direct graph inputs reviewed: L181 supplies the actual last-block endpoints; L201 supplies the candidate and sufficient phase implication. The factor count is proved directly without a density model. Analytic review checked real N, closed integer intervals, endpoint margins, and the restriction on W relative to R.
- Validation: `python3 scripts/heat/check_safe_factor_box.py` passed 8,000 exact candidate checks and 520 phase-selected floor-cell checks on rational endpoint examples. These are finite algebra checks, not an asymptotic phase-count inference.
- `python3 scripts/docs/check_structure.py` passed (203 nodes, 401 edges); `git diff --check` passed.

## Factor-slice phase reduction

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected prior changes and confirmed L202 complete; preserved existing work. Saved a [checkpoint](../drafts/heat/2026-09-14-factor-slice-phase.md) before the calculation.
- Proved [L203](../lemmas/L203-factor-slice-quartic-phase-reduction.md), a scoped part of phase counting: uniform quartic slice approximation and an interior/expanded interval count sandwich. The aggregate lower bound remains unproved.
- Archived the [quadratic slice approximation obstruction](../ATTEMPTS/2026-09-14-quadratic-factor-slice-approximation.md). This concerns absolute approximation, not a negation of the population target.
- Decision: estimate the aggregate interior quartic count, retaining actual coefficients and integer slices. PROOF.md is unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Direct inputs reviewed: L202 supplies the actual box and uniform interval bounds; L201 supplies the signed Q-minus-square-root remainder. Taylor bounds and fractional-part inclusions are proved directly.
- Validation: `python3 scripts/heat/check_factor_slice_phase.py` passed 10,200 exact rational checks. Analytic review checked fixed positive t, real N rounding, uniform remainder constants, and unique slice indexing. These are not asymptotic population evidence.
- Structural validation: `python3 scripts/docs/check_structure.py` passed (204 nodes, 403 edges); `git diff --check` passed.

## Positive proportion in the quartic factor slices

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected existing changes and confirmed L203 complete; preserved all prior work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-quartic-slice-population.md) and a resume action before completing the estimate.
- Proved [L204](../lemmas/L204-positive-proportion-in-quartic-factor-slices.md): the requested interior quartic count has a uniform positive proportion in every actual slice, hence order N³ in aggregate. The third-derivative test already recorded in foundations supplies cancellation on these shorter slices; an explicit Fourier minorant handles the interval boundaries. Recorded the standard named Fejér theorem in foundations for the Fourier identification.
- Decision: the geometric occupancy target is now established under L202's W restrictions. Next address coprimality of the unique candidate with ab on the phase-selected box. Totient selection remains unresolved as well. No failed-attempt file is needed; PROOF.md is unchanged because the overall RH argument is unchanged. STATUS remains IN_PROGRESS.
- Direct graph inputs reviewed: L203 supplies the exact slices, uniform scales, and phase inclusion; L202 supplies the aggregate box count and safe-window implication. L198 and L199 were consulted only to identify the remaining arithmetic restriction, and are not direct inputs to the new proof.
- Analytic verification checked the third derivative on the entire real slice, the uniform derivative ratio, fixed-frequency exponents, the triangle's integral and Fourier normalization, absolute tail control, the order of choosing H and N, and unique tuple indexing. No numerical distribution inference is used.
- Validation: `python3 scripts/heat/check_factor_slice_phase.py` passed 10,200 exact rational checks; `python3 scripts/docs/check_structure.py` passed (205 nodes, 405 edges); `git diff --check` passed.

## Fixed-prime coprimality selection

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected existing changes and confirmed L204 complete; preserved earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-fixed-prime-coprimality.md) before completing the scoped arithmetic selection.
- Proved [L205](../lemmas/L205-fixed-prime-selection-in-factor-slices.md): exclusion of any fixed finite set of primes retains a positive proportion in every actual slice. Full coprimality is reduced to an explicit, unproved large-prime deletion bound.
- Decision: address that remaining joint divisor count next. The fixed-modulus theorem does not authorize a growing-modulus limit. This is a completed part of the requested selection, not a failed proof of the whole count; no failed-attempt file is needed. PROOF.md remains unchanged because the overall argument is unchanged; STATUS remains IN_PROGRESS.
- Direct inputs reviewed: L202 gives the box and windows; L203 gives the exact indexing and root/phase approximations; L204 supplies the third-derivative and triangle Fourier argument. Reviewed period-D normalization, support disjointness, fixed-parameter order of limits, integer-boundary margins, and the exact common-prime partition.
- Validation: `python3 scripts/heat/check_factor_slice_phase.py` passed 10,200 rational checks. `python3 scripts/docs/check_structure.py` passed (206 nodes, 408 edges); `git diff --check` passed. Finite checks validate approximation algebra, not the remaining arithmetic population claim.

## Fixed prime bands and the residual tail

- Date: 2026-09-14. CLI/model: Codex / GPT-6 (session identity).
- Inspected the existing changes and completed L205 checkpoint; preserved all earlier work. Saved a [draft checkpoint](../drafts/heat/2026-09-14-finite-prime-band.md) before the calculation.
- Proved [L206](../lemmas/L206-fixed-prime-band-joint-divisor-bound.md), a scoped part of the large-prime count: exact fixed-prime joint frequencies and a summable finite-band union bound on the actual box.
- Archived the [fixed-band to full-tail inference obstruction](../ATTEMPTS/2026-09-14-fixed-band-to-full-tail.md). Decision: isolate the residual iterated tail, since fixed-prime slice cancellation gives no uniform estimate at growing primes. The requested full deletion threshold is not proved; STATUS remains IN_PROGRESS. PROOF.md is unchanged because the overall argument is unchanged.
- Direct graph inputs reviewed: L202 supplies the box, factor scales and total count; L205 supplies the selected set and deletion problem. The exact-root derivative proof uses the named derivative and Fejér theorems already in foundations; no new external theorem is required.
- Analytic validation checked the weighted residue error, exact rounded-root congruence including endpoints, third derivative and cancellation exponent, uniformity across slices for fixed p, and finite versus growing prime summation. No computational population claim or new computational verification is needed.
- Validation: `python3 scripts/docs/check_structure.py` passed (207 nodes, 410 edges); `git diff --check` passed.
