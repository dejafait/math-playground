# 2026-09-20 — part 03

Mode: LEAN CATCH-UP. Inspected all 232 lemma/corollary statuses and associated source availability; 216 lacked sources at entry, and L028 remained conditional. Preserved the pre-existing L012/L013 edits and history files.

Partially formalized [L014](../lemmas/L014-a-positive-kernel-laplace-representation.md) without changing its hypotheses, conclusion, or informal proof. Added the occupied-cell integral evaluation, its finite sum, summability, and identification of the infinite cell sum with η(s)/s. `lake build Rh.L014` from `scripts/lean` succeeded using the pinned local project; all four printed theorem audits contain only `propext`, `Classical.choice`, and `Quot.sound`. Embedded exactly the checked source.

The sum of cell integrals is not yet identified with the weighted improper integral. L014 stays partial; the next step is that identification with absolute integrability, before handling substitution and uniform tails. The deferred research checkpoint is unchanged; no research was attempted.

Validation: `python3 scripts/docs/check_structure.py` and `git diff --check` passed; the embedded L014 code matches its checked source exactly.
