# Research history — 2026-09-11, part 08

## Parameter-uniform reciprocal-square zero tails

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected and preserved existing tracked and untracked work; L129 was complete. Saved a draft and resume checkpoint before the Jensen and tail audit.
- Completed the current action in [Lemma 130](../lemmas/L130-parameter-uniform-reciprocal-square-zero-tails.md): an explicit O_L(log R/R) full-multiset tail bound, uniform on compact real heat-parameter intervals, with no strip assumption.
- Analytic validation checked the positive first-summand lower bound at zero, Jensen's formula and boundary-radius limit, multiplicities, strict tail cutoffs, Tonelli's nonnegative interchange, and exact integral constants. No numerical certificate is required.
- Direct graph inputs reviewed: L019 supplies the positive kernel summand, L058 the integral definition and symmetry, and L063 the uniform growth estimate. L129 is only a scope comparison, not an input. PROOF.md is unchanged because the overall argument and RH gap are unchanged. No failed approach was introduced.
- Validation: `python3 scripts/docs/check_structure.py` passed (131 nodes, 249 edges); `git diff --check` passed.
- Decision: next quantify sufficient growing additive buffers using the new uniform rate and L129 under a common strip bound. No whole-plane height derivative or RH conclusion is asserted; status remains IN_PROGRESS.

## Growing additive exterior buffers

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected existing changes and the completed L130 checkpoint; preserved all earlier work. Saved the calculation before promotion in the completed [buffer draft](../drafts/heat/2026-09-11-growing-exterior-buffer.md).
- Proved [Lemma 131](../lemmas/L131-growing-buffers-for-uniform-exterior-interaction.md): conditional uniform exterior decay for buffers growing faster than sqrt(A log A), with an exact vanishing criterion for the combined majorant. The actual optimal buffer remains unproved.
- Analytic validation checked constants, full multiplicities, oscillatory buffer functions, and the positive critical-scale limit. Direct mathematical inputs are exactly L129 and L130; PROOF.md remains unchanged because the overall argument is unchanged.
- Decision: test sharpness in the class of symmetric strip multisets with the established global counting rate before claiming an optimal scale for actual interactions. No failed attempt or RH conclusion is asserted.
- Validation: `python3 scripts/docs/check_structure.py` passed (132 nodes, 251 edges); `git diff --check` passed.

## Critical-scale multiset sharpness

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected and preserved all existing changes; the L131 checkpoint was complete. Saved the construction and resume point before the tail audit in the [draft](../drafts/heat/2026-09-11-critical-buffer-sharpness.md).
- Proved [Lemma 132](../lemmas/L132-critical-buffer-sharpness-for-strip-multisets.md): one fixed symmetric multiset for each c>0 has nonvanishing exterior upward interaction at critical-scale buffers, with an exact positive limit.
- Analytic validation checked disk counts, reciprocal-square summability, simple target zeros, strict cutoffs, multiplicity factors, and geometric remainder bounds. This elementary construction has no incoming mathematical graph edges; L131 is a scope comparison only. PROOF.md is unchanged because the overall argument is unchanged.
- Decision: the global counting bound permits critical-scale crowding; next quantify what a stronger local count would buy, without assuming that count for theta slices. No failed construction or RH conclusion is asserted.
- Validation: `python3 scripts/docs/check_structure.py` passed (133 nodes, 251 edges); `git diff --check` passed.

## Local counts and logarithmic exterior buffers

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected and preserved existing changes; L132 was complete, with no interrupted calculation for this action. Saved a [draft checkpoint](../drafts/heat/2026-09-11-local-count-exterior-buffer.md) before the endpoint and limit audit.
- Proved [Lemma 133](../lemmas/L133-local-count-exterior-interaction-and-logarithmic-buffers.md): the additional logarithmic unit-interval count gives a logarithmic-over-buffer exterior bound, sufficient for decay when d/log A diverges. Neither the local count for theta slices nor necessity for actual interactions is asserted.
- Analytic validation checked half-open shell covers, multiplicities, geometric-series constants, absolute convergence, family uniformity, and oscillatory buffer functions. No computational certificate is required. The only direct graph input is L129 for the optional local height-rate conclusion; L131 is only a comparison. PROOF.md is unchanged because the overall argument has not changed.
- Decision: next test actual critical logarithmic-scale sharpness within this stronger abstract class. No failed attempt was introduced; RH remains unproved.
- Validation: `python3 scripts/docs/check_structure.py` passed (134 nodes, 252 edges); `git diff --check` passed.

## Logarithmic-buffer multiset sharpness

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected the clean working tree and recovered the saved unfinished construction in the logarithmic-buffer draft. Completed its audit without repeating the earlier local-count bound.
- Proved [Lemma 134](../lemmas/L134-logarithmic-buffer-sharpness-for-local-counts.md): one fixed symmetric strip multiset satisfies the logarithmic local count but has exterior upward interaction tending to 1/(6c) at buffers c log A. The saved draft is marked resolved.
- Analytic validation checked coordinate separation and half-open endpoints, both real signs, full multiplicities, strict exterior cutoffs, summability, the Riemann-sum constant, and vanishing reflected/future contributions. No numerical certificate is needed.
- The construction has no incoming mathematical graph edges; L133 is a scope comparison only. PROOF.md remains unchanged because the overall argument is unchanged. No failed attempt was introduced, and no theta realization or RH conclusion is asserted.
- Decision: next audit recentered Jensen estimates for the missing local count on actual theta heat slices, explicitly isolating any needed lower bound. Abstract critical-scale sharpness alone does not establish that count.
- Validation: `python3 scripts/docs/check_structure.py` passed (135 nodes, 252 edges); `git diff --check` passed.

## Recentered Jensen lower-bound audit

- Date: 2026-09-11. CLI/model: Codex; GPT-6 (session identity).
- Inspected and preserved existing L134 changes; that step was complete. Saved a draft and current resume checkpoint before completing the present audit.
- Proved the conditional reduction in [Lemma 135](../lemmas/L135-recentered-jensen-local-count-audit.md). Existing integral estimates give a horizontally uniform disk upper bound; a polynomial local maximum-to-center ratio remains an unproved sufficient hypothesis for the logarithmic local count.
- Archived the [failed inference from normalization at zero](../ATTEMPTS/2026-09-11-recentered-jensen-without-lower-bound.md). No counterexample to the theta local count is claimed. Compact-window control does not give a rate at infinity.
- Analytic validation checked geometry, full multiplicities, boundary-radius limits, the imaginary-part integral majorant, compact minima, and the distinction between relative and coarse absolute lower bounds. No numerical certificate is needed. Direct graph inputs are L058 and L063; L130 is discussed only to explain the invalid normalization transfer, and L133 identifies the target condition. PROOF.md remains unchanged because the overall argument and RH gap are unchanged.
- Decision: first test the missing estimate at λ=0 via the zeta/gamma representation, separating classical ingredients from estimates that would still require a uniform heat extension.
- Validation: `python3 scripts/docs/check_structure.py` passed (136 nodes, 254 edges); `git diff --check` passed.
