# Nonreal deflation and derivative normalization

The gap is control of the normalized growth in L258–L259, with exterior theta Laguerre signs and the main all-degree mixed positivity still independently missing. The proposed intermediate target is a derivative-normalized witness bound at a hypothetical nonreal zero, removing the uncontrolled real-center denominator. Such a bound could set a finite, location-dependent sign target for excluding that zero. Continue if deflation supplies a bounded normalization ratio and retains a forced negative witness; abandon this repair if it merely relocates the denominator or removes the zero responsible for detection.

The full overview, global DAG, checkpoint, existing diff, L258–L259 and their assessments were read; all unfinished work is preserved. L258 already handles deflation of a real zero at the center. The present test instead removes the nonreal conjugate pair and normalizes at that pair. The distinction matters: the latter removes the witness zero itself. Prior fixed-level counterexamples remain evidence, not a ban on this test.

[L265](../lemmas/L265-nonreal-deflation-retains-normalization-gap.md) gives the exact residual factor T=|G(a)/G(a+ib)| and the required product lower bound A T for a prescribed cutoff. A simple-root polynomial family has derivative modulus bounded away from zero and bounded derivative-normalized circle growth, but T tends to zero and even the exact Cauchy ratio diverges. Its deflated quotient has every Laguerre coefficient nonnegative. The original polynomial still has a negative third coefficient, so this is a failure of the bound/normalization repair, not evidence that the true witness index must diverge.

WHY IT FAILS: normalizing by the derivative at the removed nonreal zero controls G there, whereas the witness-tail argument needs G at the real center. Their ratio remains an independent lower-bound requirement. Applying the witness argument directly to G instead removes its necessary zero premise. This diagnostic does not establish that actual theta has small ratios or rule out a theta-specific lower bound; it stops the proposed purely algebraic removal of that requirement.

Decision: NEGATIVE. One bounded test resolved; zero consecutive unresolved exploration turns. The main mixed positivity and exterior Laguerre-sign gaps are unchanged. Stop derivative normalization as a denominator-free repair. The next direction tests a different anchor for Cauchy detection: whether some positive Taylor coefficient, chosen from several central derivatives using a known off-center value, can replace the potentially tiny constant coefficient. That test must quantify its tail and preserve a forced negative sign rather than assume the desired positivity. No RH candidate is recorded.
