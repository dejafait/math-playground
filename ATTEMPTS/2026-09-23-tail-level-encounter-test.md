# Tail-level encounter test — 2026-09-23

The main gap remains all-degree mixed positivity. The bounded intermediate target was whether integer tail cutoffs and the established decay estimates force actual positive maxima into L240's deficit/curvature window. Such an encounter would obstruct the proposed vanishing smoothing widths; its absence would not prove them. Continue this mechanism only if the estimates force the required simultaneous peak-height and curvature comparisons; stop it as a consequence of these estimates alone if the mesh is relatively sparse and the controlled sector excludes the window.

The redundancy check read L236–L240, their recent assessments, and the global overview and DAG. L237's fixed multiple-zero obstruction and L239's sector ratio did not assess discrete tail-level selection. Existing peak and zero-spacing failures in the heat branch concern different quantities. Existing unfinished files were retained.

[L241](../lemmas/L241-tail-level-spacing-and-peak-window-exclusion.md) proves ε_(N+1)/ε_N→0, with an explicit exponential rate, and geometric-mean heights whose best relative mass approximation tends to error 1. These heights are a mesh diagnostic, not claimed theta maxima. More decisively, any positive strict maxima in the controlled sector satisfy d_N/κ_N≥c exp(B_N/2), the opposite of L240's required limit zero. No numerical peak extrapolation is used.

WHY IT FAILS: decreasing tail masses cross fixed small heights but give no relative proximity because consecutive masses differ by an exponentially large factor. Absolute decay upper bounds do not repair the absent peak-height correlation or supply a curvature lower bound. On the sector where a relative comparison is proved, the deficit dominates curvature exponentially. This stops the proposed spacing-plus-decay inference, not the possibility of special actual-theta encounters elsewhere and not research globally. The required global upper bound a_N^*→0 and all higher-order positivity remain missing.

Decision: NEGATIVE, one bounded test resolved, zero unresolved exploration turns. Park further smoothing-threshold diagnostics absent a new quantitative peak mechanism. A materially different bounded discovery direction is to test a positive-measure representation of the actual reciprocal-zero generating function against the signed logarithmic recurrence, checking prior failures before calculating; that directly addresses the mixed-positivity interface instead of approximation extrema. No RH candidate is recorded.
