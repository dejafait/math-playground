# Explicit fixed-h complement test — 2026-09-24

Gap: global exterior Laguerre signs at indices of order log a, needed but insufficient for L266's full witness target. Intermediate target: make the constants in the small-argument and mixed-sign absolute estimates explicit and compare them with exp(Φ_*)/a at fixed h. A positive result would assemble L288 into a global sign band. Lower indices and bounded exterior heights remain separate gaps.

Test: require the complement ratio to tend to zero for an explicit fixed h. The suggested adjustable cutoff includes the existing choice η=1/4; first test whether that choice already suffices. Continue if an explicit decay rate beats the a^6 strip-and-normalization loss; otherwise investigate a changed cutoff or signed complement mechanism. L279 and L284 only state logarithmic-h results, L287 requires diverging h, and L288 leaves this complement open. This is a quantitative extension, not a reassertion of those conclusions.

Working calculation: for |m−r|≥r/4, ψ(m−r)≥r/4 eventually. Splitting its exponential in half yields exp(−hr/8). In the central part of the small-argument region, the cosh penalty is at least h exp(r/2)/4. In the central mixed region outside it, the true polynomial gives (4/5)^(2n), bounded by exp(−hr/8) when h≥9. Thus the original cutoff already suggests the sufficient threshold h>96 because r~(log a)/2.

Result: [L289](../lemmas/L289-explicit-fixed-h-global-positivity.md) proves the complement bound with exponent −hr/8 and the global asymptotic on every fixed compact h-interval above 96. The ratio O(a^(6−h₀/16)(log a)²) meets the exact little-o threshold. Thus no cutoff adjustment was needed for the explicit-band test. ADVANCE extends global signs to some indices of order log a, but leaves smaller indices and bounded exterior heights unresolved. Continue by optimizing the cutoff-dependent decay rather than treating 96 as an intrinsic obstruction. Zero unresolved exploration turns; no RH candidate.
