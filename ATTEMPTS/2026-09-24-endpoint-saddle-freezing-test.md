# Endpoint saddle freezing test — 2026-09-24

Gap: the endpoint arithmetic coefficient must have a positive margin exceeding L302's O((1+r)²exp(−r/256)) error. Target: localize L303's Mellin integral on c=1/2+1/r and bound the error from freezing both zeta factors at v=0. A relative error smaller than the resulting modulus square would supply the endpoint sign; smaller indices and bounded exterior heights would remain unresolved.

Redundancy check: L298 proves only a fixed-head expansion and its nonuniformity, while L297 rules out arbitrary-vector Gram positivity. Neither quantifies this normalized Mellin kernel's localization or the total coefficient error for freezing at real part 1+1/r. Existing uncommitted L302–L303 and documentation changes are preserved.

Saved reasoning: the kernel width is of order r^(−1/2), but the absolutely convergent zeta series at real part 1+1/r carries logarithmic indices of order r. Small vertical displacement need not make that arithmetic factor slowly varying. Compare the full sum of absolute coefficient differences with the square's guaranteed lower bound before interpreting saddle localization as a positive asymptotic.

Result: [L304](../lemmas/L304-endpoint-saddle-localization-and-freezing-loss.md) proves a bounded absolute kernel mass, a localized tail O(r²exp(−(log r)²/8)), and an exact coefficientwise absolute freezing loss r²+O(r). The square has an elementary uniform lower bound (r+1)^(−2), so this certificate misses even a relative comparison by four powers of r. At a=0 the true difference is −r²+O(r), explicitly refuting uniform-in-height small freezing; this is not an evaluation at a_n. The localized tail alone is also larger than L302's exponential error scale, though it would be harmless against a proved polynomial margin.

WHY IT FAILS: kernel localization does not make the r-dependent zeta factors slowly varying. Their full frozen coefficient mass is of order r², whereas the exact weighted mass is of order r. The summed absolute difference therefore cannot be made small by narrowing a saddle neighborhood and discarding phases. This stops the absolute freezing certificate, not the Mellin identity, a signed comparison at a_n, or endpoint positivity itself. The next direction retains the exact weights and tests dyadic off-diagonal cancellation against the diagonal, because that uses the actual prescribed arithmetic phases rather than a uniform absolute replacement. NEGATIVE; zero consecutive unresolved exploration turns, no new global sign range, and no RH candidate.
