# Real-phase interpolation test — 2026-09-24

Gap: exterior actual-theta Laguerre signs between L285's logarithmic band and L277's range. Target: a uniform positive global asymptotic for log a≤h≤2√a(log a)². Downstream use: connect the two established sign ranges toward L266's witness cutoff; lower indices and bounded exterior heights remain unresolved.

Test: every relative error must tend uniformly to zero at the exp(Φ_*)/a scale. L278's fixed R=a^(−2/5) gives a local error ahR⁴≤2a^(−1/10)(log a)², so it can reach the upper endpoint. L282's h²/√a bound cannot do so: at that endpoint it grows. Test a split at h=a^(1/8), using signed sectors below it and L269's fixed-square absolute exterior estimate above it. This is not a direct extension of L282 alone, nor a claim that L278's low-h absolute obstruction disappears at logarithmic h. Existing interpolation L277 starts too high; L285 ends too low. Historical route stops are not additional admission conditions.

Working checkpoint: the local real-phase amplitude estimates depend on r→∞ and n/r²=O(h/r), not h=O(log a). The low-h sector envelopes and summability require h≥log a; their explicit h² losses are harmless below a^(1/8). Above that split, exp(−c h ε²) for fixed ε absorbs a^6 and log factors. Check square containment and exact n-overlap before claiming a bridge.

Result: [L286](../lemmas/L286-real-phase-global-band-interpolation.md) proves the uniform bridge. The local error is O(a^(−1/10)(log a)²); the signed complement below the split has relative bound at most O(a^(−1/4)); above the split the absolute remainder is polynomial in a times exp(−c a^(1/8)). Higher-index clipped patches remain bounded by O(h²2^(−h)). Exact h-overlap with L277 avoids rounding an asymptotic endpoint into a nonexistent gap. ADVANCE; zero consecutive unresolved exploration turns. The next direction addresses the remaining lower threshold: the absolute theta-model transfer contains a exp(−γh)/h and cannot simply be extrapolated to h=o(log a). Retaining its oscillation is a materially specific test, not an assertion of the missing signs.
