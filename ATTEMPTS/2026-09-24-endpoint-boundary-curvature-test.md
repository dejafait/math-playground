# Endpoint boundary curvature test — 2026-09-24

Gap: the endpoint same-sign strips are missing from the phase-sum assembly; L266 still requires low-index signs. Target: a summed signed strip bound o(exp(Φ_*)/a). Its downstream use is to extend L300's approximation over both entire same-sign quadrants, conditional on the separate arithmetic lower bound and mixed-sector estimates.

Redundancy and failures: L295 uses a domain-independent summable index weight and excludes h=5. L300 retains moving lower limits but only treats the interior sector. L297's indefinite stationary matrix and L298's nonuniform fixed-head expansion do not decide this strip estimate. No arithmetic positivity inference is proposed.

Discriminating test and saved reasoning: split u≤r/4 at v=3r/2. Below that cut, u+v≤7r/4 and the exact power has an exponential curvature saving relative to its tangent. Above it, v−r+log k≥r/2 gives a superexponential theta tail. Retain the lower limits −r+log j when summing variation; the harmonic head then costs O(r), not infinity. Continue if the first and mixed derivatives and rectangular edge terms retain these savings, yielding O(r² exp(−c r))/a after signed primitives. Abandon this certificate if differentiation or the index sum removes the saving. Arithmetic lower bounds, mixed sectors, lower indices and bounded heights remain explicitly unresolved.

Result: [L301](../lemmas/L301-endpoint-same-sign-boundary-curvature.md) proves the target with normalized error O((1+r)²exp(−r/256)). Polynomial derivatives through order two retain the curvature saving below the cut, while the upper piece has a superexponential theta tail. Moving lower limits make the summed variation finite. ADVANCE closes this endpoint sector gap, without extending the global sign range or supplying an RH candidate. The next direction is the endpoint mixed sectors, since those are the remaining analytic regions outside the phase-sum approximation. Zero consecutive unresolved exploration turns.
