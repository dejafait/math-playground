# Same-sign boundary variation — 2026-09-24

Gap and target: bound the same-sign region min(|s+x|,|s−x|)≤r/4 by o(exp(Φ_*)/a), uniformly on fixed compact h-bands above 5. This would supply the remaining region for a subsequent assembly of L288 and L294. Smaller indices and bounded heights remain independent gaps to L266 and RH.

Discriminating test: replace the singular n/s derivative estimates near s=0 by a tangent-exponential bound on the polynomial and its first two derivatives. Continue if the resulting exact-summand variation is dominated by summable weights (jk)^(4−h) and tends to zero on the boundary strip for each fixed pair. Abandon this certificate if the endpoint terms or index tails defeat that domination.

Redundancy check: L279 treats growing logarithmic h with an absolute bound; its a^6 loss does not settle these fixed bands. L288 only estimates arguments above r/4 and uses n/s there. L294 treats opposite-sign arguments. The new test extends the same-sign amplitude derivative bound through s=0 rather than repeating an absolute-envelope estimate. Existing unfinished changes are preserved.

Saved reasoning: in positive argument coordinates u=s+x, v=s−x, put y=u−r+log j and z=v−r+log k. The normalized polynomial ((u+v)/(2r))^(2n) and its derivatives of order at most two are bounded by C exp((h−9/2)(u+v−2r)). The exact summand and mixed derivatives should then have envelope C(jk)^(4−h) M_h(y)M_h(z), with M_h(t)=(1+exp(2t))exp(h t−(h/2)(exp(2t)−1)). For fixed j the strip u≤r/4 moves to y≤−3r/4+log j, whose envelope integral and endpoint value tend to zero. Two bounded phase primitives supply the necessary 1/a. Finite rectangles and their edge terms must be checked before asserting the result.

Result: [L295](../lemmas/L295-same-sign-boundary-variation-bound.md) establishes the target. The polynomial derivative bound removes the singular n/s estimate; the full quadrant has a summable majorant (jk)^(−(h₀−4)). On boundary strips each fixed-index variation tends to zero, and the same majorant controls the growing-index tail. Explicit rectangular integration by parts retains the finite upper-edge terms and supplies 1/a. The achieved summed signed bound is precisely o(exp(Φ_*)/a), rather than a weakening with a residual power of a. This is ADVANCE toward the fixed-h complement gap, not an RH candidate or an asserted global sign extension. Continue to global assembly because the three sector estimates now have matching parameter bands and scales. The limit h→5 and smaller indices remain outside the summability argument. Zero consecutive unresolved exploration turns.
