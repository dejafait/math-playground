# Current best argument — incomplete

The write-up establishes partial results, not RH. Current research status and the next task live in [PROGRESS.md](PROGRESS.md). The sole node-and-edge definition and lemma navigation live in [DAG.md](DAG.md); full statements and proofs live in individual lemma files. Standard definitions and named starting theorems are in [foundations](foundations/notation-and-inputs.md).

## Assembly and unresolved gap

The current main route begins with the classical zero localization and theta representation, proves the growth required for an unconditional Hadamard product, and translates that product into summable reciprocal-zero nodes. Lemmas 30–32 then prove that all mixed polynomial forms being nonnegative would force the nodes to be real and hence prove RH. This is a proved equivalence, not a proved positivity assertion.

For the actual theta moments, the analytical estimates prove six individual power-sum signs and a degree-two Jensen test. The interval certificates additionally prove H_1 and H_2 positive definite, under the explicit arithmetic contracts. No step extends these finite results to arbitrary degree. Proving that extension—or finding a different argument that excludes every off-line zero—is the unresolved gap. The exact theta transformation and boundary identity currently justify the representation and ordinary moment positivity; no proved step transfers that positivity through the signed logarithmic recurrence. The [theta audit](ATTEMPTS/2026-09-20-theta-logarithm-relevance-audit.md) now parks technical extension of the theta/Hankel route: its arithmetic reopening review found no recorded all-degree sign mechanism in either the exact Gaussian identities or the Euler-product input on its valid domain. The reduction remains available for reuse if such a mechanism is supplied.

Counterexamples retained in the lemma chain and archived in ATTEMPTS rule out the tested shortcuts: symmetry alone, continued prime positivity, positive Laplace/Fourier kernels alone, all scalar power-sum signs alone, and coarse strip geometry alone do not supply the missing mixed positivity. None is a counterexample to RH itself. The [alternating-interval screen](ATTEMPTS/2026-09-09-positive-laplace-kernel.md) also parks extension of L014: its exact arithmetic support and convergent tails supply no established noncancellation estimate throughout the right half of the critical strip. The generic L015 counterexample does not rule out a theorem exploiting that support.

The later comparison-kernel results strengthen these obstructions: even strict square-root log-concavity, superexponential decay, the established generic zero geometry, and any fixed finite number of positive Hankel tests can coexist with nonreal zeros. The comparison function may depend on the number of tests; none is asserted to pass every degree. These are counterexamples to proposed sufficient conditions, not to RH. The [comparison energy audit](ATTEMPTS/2026-09-20-comparison-energy-transfer-audit.md) also finds no transfer of L053–L054 to actual Ξ: the theta representation supplies neither the required auxiliary eigenvalue equation nor the comparison Dirichlet boundary condition for its auxiliary transform J.

The later heat/Fourier branch does not currently bridge this gap. L230 reduces an interior discrepancy to F_H=o(B_0), but L231's best recorded bound allows a relative error O((log N)^2). Even that missing cancellation would not supply the uniform center control or the whole-plane height evolution needed for a heat proof: L154 gives only average information, and L126–L128 explicitly leave the moving-supremum step unresolved. This branch is parked pending a concrete completion mechanism; its established results remain in the canonical DAG. The [relevance audit](ATTEMPTS/2026-09-20-heat-fourier-relevance-audit.md) records the distinct thresholds and the total-versus-interior discrepancy qualification.

The [standard-input portfolio audit](ATTEMPTS/2026-09-21-standard-input-portfolio-audit.md) finds no justified technical reopening in the recorded inputs: the available transforms and bounds have already been used without supplying the missing sign or uniform lower bound. This is a limitation of the current argument, not an impossibility theorem. The [primary-source screen](ATTEMPTS/2026-09-21-theta-moment-literature-screen.md) additionally finds that adjacent-moment inequalities and the screened shifted-Jensen ranges do not supply this premise: even combined with finite-degree results, they leave infinitely many unshifted degrees uncovered. The full universal-factor review in the same screen finds restricted weighted adjacent inequalities and conditional zero preservation, with no established passage to arbitrary signed Newton forms; this source is parked as a completion mechanism. The subsequent uniform curvature proof in L055 establishes strict square-root log-concavity for all sufficiently small positive parameters, so L057 now rules out that stronger shape assumption even together with any fixed finite number of Hankel tests. L055 now independently proves the full admissible weighted adjacent-moment family for that same comparison kernel, including infinite factors and every real heat parameter. Thus L057 rules out even this entire weighted family plus any fixed number of Hankel tests as a generic sufficient condition for real zeros; additional actual-theta information would be essential. The same source screen now checks the shifted-Jensen proof mechanism: its coefficient-estimate domain excludes the unshifted high-degree endpoint, so it supplies no technical reopening of the main route. The bounded nonasymptotic follow-up likewise finds no missing sign theorem: associated-kernel positive definiteness remains a premise, while the other screened statements cover asymptotic indices, fixed-level curvature, or another equivalent formulation. No new input is imported into the argument. The subsequent stop/go audit in the portfolio assessment finds no justified technical continuation from these sources; none repairs the separate gaps in the alternative routes.

The first associated-kernel calculation in L233 isolates its actual-theta Fourier spectrum as Ξ′²−ΞΞ″. The weighted-autocorrelation decomposition contains a signed second-derivative term, and an explicit positive Gaussian mixture makes the total spectrum negative. Thus this generic decomposition supplies no automatic positivity; its actual-theta sign at every real argument and the higher-order conditions remain unproved. L234 further rules out positivity of the even finite theta truncations: their derivative jump forces an eventually negative first associated spectrum. The infinite theta transformation cancels that jump, so this obstruction cannot be transferred to actual Ξ. L235 shows that smooth modular averaging of whole-line finite sums also fails as a Fourier approximation: it converges on compact intervals but its total integral diverges to −∞, whereas the actual kernel has positive finite integral. Neither failure rules out other approximation mechanisms. L236 proves that Gaussian convolution does repair the first associated spectrum of each finite reflected sum above a finite positive smoothing threshold. Passing this inequality to theta still lacks a vanishing-width estimate, and the multiplier preserves all complex zeros; no higher-order positivity follows. L237 proves that the omitted tail acts locally like a small constant: a negative-leading even-multiplicity real zero, if present, would force these thresholds to diverge. Thus compact truncation-error bounds alone do not justify vanishing widths; no such actual-theta zero is asserted. L238 gives a positive rational profile, with two controlled derivatives, for the omitted tail at frequencies proportional to π(N+1)². L239 uses the analytic theta strip to make the full transform negligible with two scaled derivatives on 3≤t≤4, transferring this profile to the finite-transform ratio. The sector supremum is B_N^(−2)(1/32+O(B_N^(−1))), which tends to zero; the global smoothing threshold and higher-order positivity remain unresolved. L240 gives an exact deficit/curvature test at positive maxima: sufficiently shallow sub-tail peaks would force large smoothing thresholds. No sequence of actual-theta peaks satisfying that condition is established. L241 shows that consecutive omitted-tail masses have ratio tending to zero and excludes this shallow-deficit window throughout the controlled sector 3≤r/B_N≤4. Tail spacing and the recorded decay estimates therefore do not supply the proposed encounter mechanism; additional peak-height and curvature information would be needed.

The squared imaginary-axis logarithmic derivative in L242 has an exact theta double-integral formula, but its kernel takes both signs on actual positive-weight regions for every parameter. A positive Stieltjes representation would supply the missing mixed forms; direct pointwise positivity does not construct it. Even the integrated first-derivative sign is not established by this test. L243 also blocks the proposed positive-curvature variance argument for the cosh-tilted density: its curvature becomes negative near zero for large parameters, and the auxiliary pointwise comparison fails even at zero tilt. L244 shows that whole-line exponential tilting preserves curvature but still leaves strictly positive integrated inverse-curvature slack near zero tilt, so that upper-bound comparison also fails. L245 uses the completed-zeta formula and absolutely convergent Dirichlet bounds to prove m(r)−r v(r)>2/5 for r≥100, hence G′(x)<0 for x≥10000. L246 closes the compact first-sign range using the existing zero sector: translation by x≥0 preserves its angle, proving G′(x)<0 everywhere and six alternating logarithmic-derivative signs on the whole positive axis. This completes the first-sign target without moment quadrature. L247 shows that L029’s nonreal-zero examples satisfy even strict complete monotonicity on the whole positive axis, yet have no positive Stieltjes representation. Thus all scalar derivative signs alone cannot supply the mixed forms; actual-theta complete monotonicity and the stronger Stieltjes/all-degree threshold remain open. L248 proves the strict Stieltjes imaginary-part sign throughout the right upper quadrant from the existing zero sector, while its exact theta double-integral kernel has both signs. The nonreal-zero examples pass this quadrant test too; full upper-half-plane holomorphy and sign remain missing. L249 extends the sign to left-side points −t+iε with ε²≥t+1/2, but this pairwise threshold is sharp under the recorded localization for t≥16. It leaves arbitrary fine boundary resolution uncontrolled; any nonreal pole would force a sign violation just below it. Geometry alone therefore does not complete this bridge. L250 derives the exact modular boundary-flux decomposition, but its boundary contribution is strictly positive on a proved small left-side rectangle. Thus termwise boundary signing fails; the residual integrated cancellation and full upper-half-plane sign remain unproved. L251 shows that adding real roots at the real centers of L247’s nonreal pairs yields strict first Laguerre positivity on the entire real axis while retaining complete monotonicity and the recorded localization. These combined generic conditions therefore still do not force real zeros; no positive Fourier-kernel representation is asserted for this example. L252 proves that its second generalized Laguerre expression is strictly negative at 10, so the next level detects this particular masking; no actual-theta second-level sign or all-degree implication is obtained. L253 identifies the actual-theta second associated spectrum as 3D₂(Ξ;x). Its autocorrelation remainder has a Fourier transform of both signs, so separate nonnegative signing fails even for actual theta; a quantitative compensation estimate and the higher levels remain open. L254 derives the unsplit double theta-series spectrum and proves that pairing the two index orders leaves eventually negative (1,m) blocks for every m≥2. Exact modular boundary cancellation therefore does not supply termwise positivity; larger regroupings and the total second-level sign remain open.

L255 proves every generalized Laguerre coefficient D_n(Ξ;x) is strictly positive for |x|≤7/2 by grouping conjugate zeros in the unconditional product. This covers all levels on a compact interval, but leaves the exterior real range uncontrolled: near a possible nonreal zero’s real center, its individual block can have a negative coefficient. Compact all-level positivity is not global associated-kernel positivity or the missing mixed-form theorem. L256 gives the exact second-level compensation threshold E_P+E_N≥ST for positive and negative zero blocks. Its polynomial test shows that adding a positive real-root block can worsen the second-level sign through the negative cross term; positive block mass alone supplies no extension. No actual-theta estimate meeting this threshold is established. L257 shows that real-root multiplicity can mask any prescribed finite number of global Laguerre levels while retaining nonreal zeros, even in an even polynomial with the recorded coarse localization. Thus global second-level nonnegativity alone is not a generic real-zero criterion; this example supplies neither a positive Fourier kernel nor strict signs at its repeated real roots. L258 gives an explicit Cauchy-growth bound on a negative Laguerre witness index forced by any specified nonreal zero, including when its real center is itself a zero after deflation. This replaces a fixed finite target by a location-dependent one; actual-theta positivity through that index outside the compact interval and control of the normalized growth ratio remain unproved. L259 specializes the circle maximum to the explicit theta bound Ξ(iR)², independent of the real center. This makes the witness index explicit but leaves an uncontrolled central-value denominator and supplies no exterior sign theorem. L260 splits the triple real root in the second-level masking example and obtains strict global first- and second-level positivity with only simple zeros, including in an even localized example. Thus simplicity and strictness do not repair this finite-level sufficiency inference; no actual-theta sign or positive Fourier kernel follows. L261 extends strict simple-root masking to three global levels by a five-root split, including an even localized example. This still supplies neither a positive Fourier kernel nor an arbitrary-finite-level simple-root theorem. L262 supplies a strictly positive Gaussian-times-polynomial Fourier kernel after multiplying this example by −exp(−z²), preserving its simple nonreal zeros and three strict global levels. Thus kernel positivity does not repair that generic finite-level criterion. The multiplier raises the order to two. L263 replaces it with a high even sinc power and proves a nonnegative compact spline kernel of order one with three nonnegative global levels. Its repeated new real zeros destroy strictness, and the large-width bound does not preserve coarse localization; L264 then certifies width 3/4 and stable unequal-width perturbations, recovering simplicity, coarse localization, and three strict global levels with an order-one compact positive-interior kernel. Thus that combined generic finite-level criterion also fails; actual-theta structure and all-degree positivity remain missing. L265 tests nonreal-pair deflation in the quantitative witness bound: derivative normalization retains an uncontrolled quotient value ratio, and a simple-root polynomial family makes the exact Cauchy ratio diverge despite bounded normalized growth. Deflating the pair also removes the zero that forces detection. This stops that algebraic normalization repair, without excluding a different witness estimate or a theta-specific lower bound. L266 replaces the central denominator by a positive off-center anchor on Re s=2 and a finite coefficient-sum comparison. It gives an explicit height-only negative-witness cutoff, including at real centers that are zeros, without any central derivative lower bound. This removes the normalization obstacle for that witness target; actual-theta exterior positivity through the resulting cutoff remains unproved. L267 supplies a quadratic cosine/variance sufficient test for those signs. L268 proves V_n≥c/(n+1), which makes that test fail at every index through the linear cutoff for all sufficiently large heights. This stops the quadratic concentration certificate, not the actual-theta signs or methods retaining oscillatory cancellation. L269 retains that cancellation and proves eventual strict Laguerre positivity uniformly when n/|a| lies in any fixed compact subinterval of (0,∞), using a positive complex-saddle asymptotic with controlled local and complementary-contour errors. This covers proportional indices at large heights, but leaves n=o(|a|) and bounded heights unresolved; it does not establish every sign through L266’s cutoff. L270 extends the controlled saddle range down to n≥|a|^(4/5)(log |a|)² by resizing the local square and absorbing the polynomial strip-bound cost. This leaves a sublinear but diverging set of lower indices, as well as bounded heights, unresolved. L271 integrates the quartic and squared-cubic Gaussian corrections and bounds the remaining Taylor error, lowering the sufficient threshold to n≥|a|^(3/4)(log |a|)³. The corrections are O(1/|a|), but the controlled remainder still requires a diverging lower threshold; low indices and bounded heights remain open. L272 rotates the local quadratic form to real Gaussian decay of order |a|, but proves that strip-contained square patches capture its full mass only for n≫√|a| log |a|. This restricts that particular contour mechanism; connecting boundaries and nonlinear errors are uncontrolled, so it establishes no further positivity range. L273 shows that at h~√|a|(log |a|)² each connecting face of the simultaneous square rotation has absolute mass comparable to the signed main term, even with uniformly small actual-theta model error. Discarding these faces by absolute estimates fails. L274 proves that their oriented sum is nevertheless o(exp(Φ_*)/|a|), first by opposite Fresnel tail estimates and Stokes for the quadratic model, then by the uniform theta-model error. This resolves the local connector estimate at that scale. L275 bounds the surrounding unrotated square annulus by o(exp(Φ_*)/|a|), using the exact separable imaginary phase and a uniformly bounded mixed variation of the real amplitude. L276 assembles the inner patch, signed connectors, annulus, and far exterior uniformly for √|a|(log |a|)²≤h≤2√|a|(log |a|)², proving strict positivity in the corresponding band n of order √|a|(log |a|)³. L277 freezes the inner radius at κ(log |a|)²/√|a| and checks uniform connector, annular, and exterior errors across the intervening range. Together with L271 this proves eventual positivity for ceil(√|a|(log |a|)³)≤n≤C|a| for every fixed C>0. Smaller indices and bounded exterior heights remain unresolved; the full L266 witness target is still unproved. L278 uses an exact real phase change to obtain the positive local saddle asymptotic at log a≤h≤2 log a, corresponding to indices of order (log a)². Transformed amplitude variation, local boundary tails, and theta-model error are controlled at o(exp(Φ_*)/a), but the exterior of the fixed saddle squares remains uncontrolled at that scale. Thus this local result does not extend the global sign range. L279 bounds the entire small-argument region min(|s+x|,|s−x|)≤r/4 by o(exp(Φ_*)/a) even at logarithmic h, absorbing the global strip loss through exp(−c h r) decay. The remaining exterior still lacks signed control; the global sign range is unchanged. L280 locates each fixed higher-index positive-argument stationary point and proves its individual signed local contribution is o(exp(Φ_*)/a), with an explicit decaying weight. Uniform summation over growing indices, reflected sectors, and the nonstationary complement remain uncontrolled; this does not extend the global sign range. L281 supplies a uniform summable little-o bound at the principal scale for higher-index stationary patches whose two central real arguments are at least r/4, retaining the exact summands. Reflected sectors, centers outside this range, and summand-wise nonstationary complements remain uncontrolled; the global sign range is unchanged. L282 bounds the summed signed nonstationary complements in the positive-positive argument sector by O(exp(Φ_*)h²/a^(3/2)) at logarithmic h, using a global separable envelope with summable index weights. Clipped stationary squares at this sector boundary and the other sign sectors still require control; the global sign range remains unchanged. L283 resolves the clipped-square comparison, including centers outside L281’s restriction: the summed higher-index clipped contributions are O(exp(Φ_*)h²2^(−h)/a), and their difference from the restricted full-patch sum is o(exp(Φ_*)/a). This completes the positive-sector partition at the principal scale. Other sign sectors, lower indices, and bounded exterior heights remain unresolved; no new global sign range follows.

L284 bounds both mixed-sign argument sectors in absolute value by Cδ^(−5)(r+1)²exp(Φ_*−c h r)=o(exp(Φ_*)/a) at logarithmic h. Retaining the true polynomial factor supplies the decay that the swapped envelope maximum loses. L285 now assembles the whole plane, checks the actual-kernel versus first-summand patch correction and exact reflection factor, and proves the positive global asymptotic uniformly for log a≤h≤2 log a. This supplies a band of indices from (1/2+o(1))(log a)² to (1+o(1))(log a)². L286 extends the real-phase local estimate and splits the exterior treatment between signed sector estimates and fixed-square absolute decay, proving a uniform bridge to L277. Consequently all indices from ceil(r(log a)(log a−9/2)), asymptotic to (log a)²/2, through C|a| have eventual positivity for each fixed C>0. The diverging set of smaller indices and bounded exterior heights still prevent the full L266 witness target and any RH conclusion.

L287 retains the exact first theta summand in the local oscillatory integral and extends the global estimates to every diverging h≤log a. The exact-factor signed correction is O(exp(Φ_*)/a²), avoiding the previous absolute full-kernel transfer loss. Combined with L286, this proves eventual positivity along every sequence n/log |a|→∞ with n≤C|a| for fixed C. Indices of order log |a| or smaller and bounded exterior heights still prevent the full L266 witness target; no RH candidate follows.

L288 evaluates the full positive-positive sector at h in any fixed compact subinterval of (5,∞). Its leading coefficient is |ζ(h−4+ia)|², bounded below uniformly by an absolutely convergent Euler-product argument. Thus higher-index stationary terms need not be negligible to obtain a positive sector main term. The remaining sectors are still uncontrolled at the required scale for general fixed h, so this does not extend global Laguerre positivity or close L266’s witness target.

L289 makes the absolute complement decay explicit as exp(−hr/8), overcoming the strip and normalization loss for h in any fixed compact interval above 96. Together with L288 this yields global positivity in these fixed-h bands, hence some indices of order log |a|. Smaller logarithmic and sublogarithmic indices and bounded exterior heights still block L266’s full witness target; no RH candidate follows.

L290 retains the polynomial penalty and partitions the complement by the sum of its envelope coordinates. With a variable fixed sector cutoff, this lowers the sufficient global fixed-h threshold to h>9/2+15/(4 log 2), approximately 9.91. The complement ratio is O(a^(6−λ/2)(log a)²)=o(1) for a chosen λ>12; L288’s sector calculation is rechecked at the new cutoff. This bound still does not reach h=5, and smaller logarithmic indices, sublogarithmic indices, and bounded exterior heights remain unresolved.

L291 tests the sharper argument-dependent theta strip majorant on a fixed mixed-sector box. Its normalized mass is comparable to exp([5/2−2(h−9/2)log 2]r), so this absolute certificate still fails for 5<h≤9/2+5/(4 log 2), approximately 6.303. This is a limitation of that majorant, not a lower bound on the actual integrand or a negative Laguerre sign. The global sign range is unchanged; signed mixed-sector control remains a possible alternative.

L292 identifies interior stationary points in the exact reflected mixed-sector phases, ruling out a whole-sector nonstationary derivative certificate. The obstructing box of L291 itself is uniformly nonstationary, so this does not preclude signed control there. Summed stationary-neighborhood and complementary error estimates remain missing; the global sign range is unchanged.

L293 bounds the summed absolute masses of mixed-sector summand-wise stationary cores of radius r/sqrt(a) by O(exp(Φ_*)r² exp(−γr)/a), with γ>0 on every fixed compact h-band above 5. This includes clipped boundary cores and growing indices. Their signed complements and boundary terms remain uncontrolled; the global sign range is unchanged.

L294 uses globally bounded oscillatory primitives and a summable mixed-amplitude variation bound to control both whole mixed sectors by O(exp(Φ_*)exp(−γr)/a), uniformly on compact h-bands above 5. Subtracting L293 also bounds their summand-wise complements at the required little-o scale, including sector boundaries. The small-argument same-sign regions and the global fixed-h assembly still require checking before any extension of L290’s sign range; lower indices and bounded exterior heights remain unresolved.

L295 bounds the small-argument strips in both same-sign sectors by o(exp(Φ_*)/a), uniformly on compact h-bands above 5. A tangent-exponential bound controls polynomial derivatives through s=0, and summable exact-summand variation with two oscillatory primitives controls the moving boundary strips, including endpoint terms. This supplies the region omitted from L288; the global fixed-h assembly and its normalization remain to be checked before asserting an extended sign range. The summability bound excludes h approaching 5, and smaller indices and bounded exterior heights remain unresolved.

L296 assembles L288, L294 and L295 over the whole plane, with the exact reflection factor and a uniform positive zeta coefficient, proving global Laguerre positivity on every fixed compact h-band above 5. Thus every fixed logarithmic-index band c₀ log |a|≤n≤c₁ log |a| with 1/4<c₀<c₁ has eventual positivity. The endpoint h=5, smaller indices, and bounded exterior heights remain outside this result; L266’s full witness target and RH remain unresolved.

L297 tests the exact interior stationary weights at h=5: their total absolute mass is asymptotic to 4r, and their matrix has a negative two-by-two principal minor. Thus retaining the weights does not restore bounded-mass domination or a general Gram-square positivity argument. The actual phase-vector sum may still be positive; its quantitative lower bound, uniform summed stationary error, and endpoint complementary estimates remain unproved. The global sign range is unchanged.

L298 gives an absolutely convergent Mellin representation and an exact integer-order differential formula for the endpoint phase sum. The Mellin factors couple different heights rather than forming a modulus square. A fixed-head curvature expansion is proved but is not uniform on the growing index set. These identities give no endpoint lower bound or extension of the sign range; quantitative stationary errors and arithmetic cancellation remain unresolved.

L299 makes the endpoint clipped stationary-patch asymptotic uniform on the growing index set. Its normalized summed error is O(r a^(−1/10)+r exp(−r/32))=o(1), including patches touching the sector boundary. This connects the separately assigned patches to L297’s exact phase sum, but does not estimate their nonstationary complement or supply a positive arithmetic lower bound. No endpoint or global sign extension follows.

L300 retains the index-dependent sector boundaries in the endpoint amplitude variation estimate. Their exponential tails give summed variation O(r²) and a nonstationary complement O(exp(Φ_*)r²/a^(3/2))=o(exp(Φ_*)/a). Together with L299 this approximates the whole positive sector by its exact endpoint phase sum with additive normalized error o(1). A positive arithmetic lower bound and the other endpoint sectors remain unresolved; the global sign range is unchanged.

L301 retains the exact polynomial curvature on the endpoint same-sign boundary strips. Splitting off the large-argument theta tail gives summed signed error O(exp(Φ_*)(1+r)²exp(−r/256)/a)=o(exp(Φ_*)/a), including boundary terms and growing indices. This closes the same-sign regions omitted from L300 at h=5. The endpoint mixed sectors and a positive arithmetic lower bound exceeding the full error remain unresolved; no global sign extension follows.

C294a rechecks L294 at h=5 and proves whole mixed-sector error O(exp(Φ_*)exp(−(log 2−1/2)r)/a). Its Gaussian-moment summation remains convergent at the endpoint; the same-sign harmonic obstruction does not apply to this calculation. This supplies the last listed endpoint sector estimate. Whole-plane normalization and an arithmetic lower bound exceeding the combined additive error remain to be assessed; no endpoint positivity or global sign extension follows.

## Partial results

The write-up establishes classical zero localization, the entire theta completion and cosine representation, a paired Hadamard product, reciprocal-zero identities, a small zero-free rectangle, six scalar signs, and a degree-two Jensen test. The actual theta kernel is also shown to be strictly log-concave with a smooth even extension. The saved interval certificates establish positive definiteness of H_1 and H_2 under explicit arithmetic contracts. Detailed statements and qualifications belong in the lemma files accessible through the DAG.

No novelty or proof of RH is claimed. The all-degree positivity assertion remains the missing theorem.

## Known traps checked

- No explicit-formula error estimate, prime-number-theorem error term, or unproved zero-free strip is used.
- No RH-equivalent positivity is assumed. Corollary 32a explicitly labels its all-degree condition as RH-equivalent and unproved; it is never used as a premise for an unconditional conclusion.
- Every infinite manipulation has a stated bound: Dirichlet/prime absolute sums, compact alternating-series tails, exponential theta tails, or summable reciprocal squares. The boundary proof uses local Taylor/Laurent bounds, not a boundary interchange of prime sums.
- There are no numerical zero computations or extrapolations from finitely many zeros. The finite determinant calculations use proved tails, proved quadrature remainders, and outward interval arithmetic, with their implementation contracts explicitly stated. An ordinary decimal value would not suffice.
- A positive kernel does not make its Fourier transform real-rooted. Reflection invariance of a set does not make every point fixed. Concrete counterexamples are retained.
- Algebraic squares in mixed forms are not replaced by modulus squares; that replacement would erase the very obstruction being tested.
- Finite positive scalar sums, a degree-two Jensen polynomial, and two finite positive Hankel matrices are not an all-degree theorem. No Li, Robin, Lagarias, or Nyman–Beurling criterion is claimed proved.
